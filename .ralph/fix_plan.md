# Ralph Fix Plan

## High Priority
- [x] Review codebase and understand architecture - **Greenfield project, bootstrapped from scratch**
- [x] Identify and document key components - **Done: opds, catalog, server packages**
- [x] Set up development environment - **go.mod created, build instructions in AGENT.md**
- [x] Implement `go.sum` and fetch dependencies (`go mod tidy`)
- [x] Implement a file-system catalog backend (scan directory for EPUB/PDF files) - **Done: internal/backend/fs/fs.go**
- [x] Connect catalog backend to server handlers (wire up real data) - **Done: server.New() now takes catalog.Catalog**
- [x] Implement EPUB metadata extraction (title, author, cover from .epub files) - **Done: OPF metadata via stdlib archive/zip + encoding/xml**
- [x] Implement book page instead of dialog - **Done: hash-based routing in Vue SPA (#/books/{id}), dedicated book detail page with cover/metadata/download/edit; GET /api/books/{id} endpoint; detail modal removed**
- [x] Implement frontend in french - **Done: all UI text translated to French (labels, placeholders, toasts, empty states, modal titles, metadata fields, pagination, upload/edit dialogs)**
- [x] The 'has been read' must be a button in book page and not in the edit form - **Done: dedicated "Marquer comme lu / Marquer comme non lu" toggle button in book page action area; isRead checkbox removed from edit modal; toggleRead() calls PATCH /api/books/{id}**
- [x] Add a filter on grid page to have only not readed book - **Done: "Non lus seulement" toggle pill in grid filter bar; ?unread=1 API param; UnreadOnly field in SearchQuery; both fs and sqlite backends filter by is_read=0**
- [x] Sort Book by added date descending by default, add possibility to sort by name, added date - **Done: AddedAt field on Book (file mod time); SortBy/SortOrder on SearchQuery; fs backend sorts by AddedAt desc on Refresh and re-sorts matched slice; sqlite adds added_at column (migration-safe) + sortClause() helper; handleAPIBooks always uses Search with parsed ?sort= param (added_desc/added_asc/title_asc/title_desc); Vue sort selector in filter bar with localStorage persistence**
- [x] Gérer les versions de schema de la base de données et quand une mise a jour de schéma est effectué faire une migration pour ne pas devoir remettre la base à zéro - **Done: PRAGMA user_version–based migration system in sqlite.go (schemaMigrations slice, migrateSchema(), currentSchemaVersion const); migration1 handles both fresh and pre-migration DBs; 3 tests (fresh, idempotent, legacy DB upgrade)**
- [x] Backup database every night - **Done: catalog.Backupper interface; sqlite.Backend.Backup() uses VACUUM INTO for a live consistent copy; pruneBackups() keeps most recent N files; BackupDir/BackupKeep fields in Config (env: BACKUP_DIR/BACKUP_KEEP, default keep=7); runNightlyBackup() goroutine in main.go sleeps until next local midnight then repeats every 24h; 2 tests (file created, prune)**
- [x] Intégre l'agent sdk claude, tu dois mettre un prompt dans l'interface et ajoute l'install de claude code dans le docker - **Done: internal/ai/agent.go implements Anthropic Messages API agentic loop with catalog tools (search_books, get_book, update_book, list_authors, list_tags); POST /api/ai/chat endpoint; ANTHROPIC_API_KEY env var / anthropic_api_key config; floating violet chat panel in Vue UI shown when aiEnabled=true; Node.js + @anthropic-ai/claude-code installed in Docker runtime stage**
- [x] Fix error "error":"API error not_found_error: model: claude-3-5-sonnet-20241022" - **Done: updated defaultModel in internal/ai/agent.go from deprecated claude-3-5-sonnet-20241022 to claude-sonnet-4-6**
- [x] Passe le projet en multi utilisateur, le mot de passe est partagé mais on choisi qui se connecte et on stocke le choix dans la session. Il faut une page d'admin pour pouvoir créer des utilisateurs et choisir le code couleur. Ce code couleur sera utilisé pour l'affichage du status lu qui doit être individualisé. Si le livre est lu par les plusieurs utilisateur il faut faire un mélange de couleur. - **Done: catalog.UserManager + catalog.UserReadManager interfaces; SQLite migration v4 (users + user_read_status tables); auth.go stores userID in session; login page shows user picker when users exist; /api/me, /api/users CRUD, /api/books/{id}/read endpoints; Vue admin page (#/admin) with user list/create/edit/delete + colour picker; current user name + colour dot in header; gear icon for admins; all read banners use readColors array for per-user colour gradient (mixing when multiple readers)**
- [x] Ajoute une classification d'age sur les livres - **Done: AgeRating int field on Book (0=non classifié, 3/6/10/12/18+); select in edit form; coloured badge on book detail page; SQLite migration v7; fs+sqlite backends updated**
- [x] Rajoute un profil enfant qui filtrera les livres par age - **Done: IsChild bool on User; CreateUser/UpdateUser accept isChild; migration v7 adds is_child to users; maxAgeRatingForUser() auto-filters books (MaxAgeRating=10) for child users; isChild checkbox in admin form; Enfant badge in user list; pink notice banner on grid page for child profiles**
- [x] Proposer de redemarrer après la mise à jour - **Done: POST /api/restart handler uses syscall.Exec to re-exec the current binary; update dialog shows a "Redémarrer maintenant" green button after apply succeeds; restartServer() polls /api/update/check until server is back up then auto-reloads; "Plus tard" button closes the dialog without restarting**
- [x] Ajoute un lecteur epub intégré - **Done: epub.js (CDN) integrated in Vue SPA; "Lire" button on book detail page (only for EPUB files); #/read/{id} route; reader view with TOC panel, font-size ±10% controls (persisted in localStorage), progress bar (0–100%), position save/restore per book (localStorage), keyboard arrows, epub.js renderTo(container, {paginated}); FileType field added to bookJSON API**
- [x] Le boutton Lire ne s'affiche pas - **Done: handleAPIBook was missing FileType in bookJSON response; added FileType via bk.Files[0].MIMEType (same pattern as handleAPIUpdateBook)**
- [x] Ajoute la categorie +16 dans la classification d'age - **Done: added 16+ (Jeune adulte) option in edit form select, case 16 in ageRatingLabel(), and v>=16 rose badge class in ageRatingBadgeClass()**
- [x] Fix : Erreur d’analyse XML : erreur de syntaxe Emplacement : https://books.jerinn.com/opds/books/f1673f384757b6c1/META-INF/container.xml Numéro de ligne 1, Colonne 1 : **Done: added handleOPDSNotFound catch-all for /opds/** that returns XML 404 instead of HTML page**
- [x] Après un redemarrage la session n’est pas conservé - **Done: catalog.SessionPersistence interface; SQLite migration v8 (sessions table); sqlite.Backend implements SaveSession/DeleteSession/LoadSessions/PruneExpiredSessions; server.sessionStore syncs to DB on create/delete and loads from DB at startup**
- [x] L'url opds/books/ef5ad688a5c8a736/META-INF/container.xml fait une 404, tu dois faire une route special qui pointe vers l'epub décompresser temporairement - **Done: GET /opds/books/{id}/{filepath:.*} route; handleEPUBFile opens the EPUB ZIP, finds the inner file by name, streams it with proper Content-Type; registered before the /opds/ catch-all; path traversal prevented**
- [x] Dans le profil enfant spécifier l'age afin de pouvoir filtrer par la classification - **Done: MaxAge int field on catalog.User; CreateUser/UpdateUser accept maxAge; SQLite migration v9 adds max_age INTEGER DEFAULT 10 to users; maxAgeRatingForUser() uses u.MaxAge (fallback 10); API handlers pass maxAge in JSON; Vue admin form shows a max-age selector (3/6/10/12/16) when isChild is checked; Enfant badge shows age (e.g. "Enfant 10+"); child notice banner shows dynamic max age**
- [x] Ajouter un filtre dans le catalogue par classification d'age - **Done: AgeRating field on SearchQuery (0=no filter, -1=unclassified, positive=exact match); fs+sqlite backends apply filter; ?age_rating= param on /api/books (ids_only mode too); Vue filter bar has a select dropdown (Tous les âges/Non classifié/3+/.../18+) that highlights pink when active; counter text updated; gridFilterParams includes age_rating**
- [x] Ajoute un date et heure de dernier traitement de maintenance des données des livres - **Done: lastMaintenanceAt timestamp on Server struct; initialized at startup; updated on manual /api/refresh + background ticker refresh; exposed in /api/ping as lastMaintenanceAt (Unix ms); Vue UI shows "Mis à jour le DD/MM/YYYY HH:MM" in filter bar counter text; refresh button tooltip also shows last maintenance time**
- [x] Le lastMaintenanceAt doit être par livre et pas sur le server - **Done: LastMaintenanceAt field on catalog.Book; SQLite migration v10 (last_maintenance_at column); set to NOW() on insertBook; bookJSON API exposes lastMaintenanceAt (Unix ms); book detail page shows "Indexé le DD/MM/YYYY HH:MM"; removed server-level lastMaintenanceAt from Server struct, /api/ping, and refresh handlers**
- [x] Le lastMaintenanceAt doit être vide sur insertBook il doit être traité après son ajout - **Done: removed last_maintenance_at from INSERT OR IGNORE in insertBook (defaults to 0); added updateMaintenanceAt(id) helper that sets it to NOW(); called after insertBook in Refresh() and StoreBook(); row reader maps 0 → zero time.Time{} so IsZero() works correctly and the Vue "Indexé le" field hides for unprocessed books**
- [x] expose le champ lastMaintenanceAt dans l'api et le mcp pour que l'agent puisse le mettre a jour - **Done: LastMaintenanceAt *time.Time added to catalog.BookUpdate; sqlite UpdateBook writes last_maintenance_at column; fs backend persists via metaOverride; PATCH /api/books/{id} accepts lastMaintenanceAt (Unix ms, -1=now); MCP update_book tool accepts last_maintenance_at integer; AI agent update_book tool accepts last_maintenance_at; formatBookDetail shows "Indexé le" date; v1.67.0**
- [x] expose la classification d'age dans l'api et le mcp pour que l'agent puisse le mettre a jour - **Done: age_rating field added to MCP update_book tool schema + handler (numericArg); AI agent update_book tool schema + handler; formatBookDetail shows "Classification d'âge: N+" when set; build passes, MCP tests pass**
- [x] Permet d'utiliser un token oauth pour l'agent claude - **Done: AnthropicOAuthToken field added to config (yaml: anthropic_oauth_token / env: ANTHROPIC_OAUTH_TOKEN); ai.NewWithOAuth() constructor sends Authorization: Bearer header instead of x-api-key; server.Options.AnthropicOAuthToken takes precedence over API key when both set; v1.70.0**
- [x] Dans le filtre par age si quand je choisi un age il faut prendre tout les age plus petit. Exemple quand je chois +12 il faut aussi afficher +6, +3, +8 - **Done: AgeRating filter changed from exact match to age_rating > 0 AND <= N in both fs+sqlite backends; Vue labels updated to "Jusqu'à N+"**
- [x] Ajoute un filtre sur les livres non indexé, met a jour le mcp et le skill pour l'utiliser - **Done: NotIndexed field on SearchQuery; fs+sqlite backends filter last_maintenance_at=0; ?not_indexed=1 API param; MCP search_books not_indexed bool; Vue "Non indexés" orange toggle pill; librarian skill batch mode starts with not_indexed:true**
- [x] Retire le point de notification dans la barre de navigation sur les recommandations et la liste de souhait quand j'ai vu la page - **Done: wishlistSeenCount + recommendationsSeenCount refs initialized from localStorage; wishlistHasNew/recommendationsHasNew computed show dot only when count > seen count; loadWishlist(true)/loadRecommendations(true) called from onHashChange to mark seen after page loads; badge conditions updated to use new computeds**
- [x] Le lien pour ajouter une recommandation doit être en haut et pas en bas - **Done: added "Recommander un livre" violet button in the recommendations page header (flex justify-between with h1), matching wishlist page pattern; clicking navigates to library where user can pick a book to recommend**
- [x] Le formulaire pour ajouter un souhait doit être en modale - **Done: removed inline add form and inline edit form from wishlist page; unified wishlist modal (add + edit) shows when wishlistEditingId !== null; backdrop closes modal; dynamic title "Nouveau souhait" / "Modifier le souhait"; button label adapts (Ajouter/Enregistrer)**
- [x] Ajoute un web manifest afin d'utiliser l'app en PWA - **Done: web/manifest.json (name, icons, theme_color, display:standalone, shortcuts); 4 PNG icons (192/512px, regular+maskable) generated via Python PIL; web/sw.js service worker (cache-first for static/covers, network-first for API); <link rel="manifest"> + apple-touch-icon + theme-color meta tags in index.html; SW registration script at end of index.html; /sw.js, /manifest.json, /icons/* served publicly (no auth) in server.go; embed.go updated to include manifest.json, sw.js, icons dir**
- [x] Quand je met a jour une couverture de livre la nouvelle image s'affiche mais quand je rafraichie la page elle n'est plus affiché. - **Done: UpdateCover in both fs and sqlite backends now stores cover_url as /covers/{id}?v={timestamp}; version parameter changes on each update so browsers/service-workers bypass stale cached copy; v1.77.0**
- [x] Passe le site en responsive pour téléphone - **Done: header OPDS buttons hidden on mobile; reader TOC becomes overlay drawer with backdrop on mobile; filter bar splits into two rows on mobile (count+sort on top, scrollable pills below); recommendations/wishlist/admin page headers stack to flex-col on mobile; modals become bottom-sheets on mobile (items-end, rounded-t-2xl); book detail action buttons shorten text on mobile; v1.78.0**
- [x] La barre d'outils n'est pas responsive sous firefox mobile - **Done: added overflow-hidden to header container + min-w-0 to search bar for proper flex shrinking; AI chat and dark-mode buttons hidden on mobile (hidden sm:flex) to free ~80px width so the header fits on 360px screens without horizontal overflow in Firefox**
- [x] Je vois que le filtre "non indexé" ignore les timestamps que j'ai mis à jour — bug côté API. Les livres déjà traités réapparaissent. Les vrais non-traités sont plus loin dans la liste. Voici les nouveaux depuis la dernière session (positions 1-2) et le reste sur les pages suivantes. **Done: Removed updateMaintenanceAt call from StoreBook in SQLite backend so uploaded books are not immediately marked as indexed; they remain in NotIndexed filter until processed by librarian skill. Added tests to verify behavior.**
- [x] Sur un navigateur de liseuse eink les couvertures n'apparaissent pas - **Done: replaced `inset: 0` CSS shorthand with explicit `top/right/bottom/left: 0` and `aspect-ratio: 2/3` with a padding-top: 150% pseudo-element technique; added `.cover-aspect` utility class for other cover containers (collection view, recommendations mini-cover); also added vendor-prefixed `object-fit` for maximum compatibility; v1.82.0**
- [x] Optimise le serveur mcp pour réduire le contexte retourner, met une limite sur le tools get tags - **Done: list_tags default limit 200→50 (max 200); list_authors default 200→100 (max 500); list_publishers default 200→100 (max 500); pagination hint message when more results available; formatBookDetail truncates summary to 600 chars; v1.83.0**
- [x] Les covers ne marche pas avec le navigateur boox neobrowser sur une liseuse de la marque boox - **Done: OPDS token embedded in cover URLs in all API responses (grid, book detail, update, recommendations) via withToken(); loading="lazy" removed from all cover img tags; v1.84.0**
- [x] Ajoute une page de statistiques de lecture par utilisateur - **Done: catalog.ReadStatsProvider interface + ReadStats/LabelCount/MonthCount types; SQLite migration v11 adds read_at to user_read_status; SetUserRead stamps current timestamp (preserves legacy read_at on re-toggle); ReadStats() aggregates totals, this-year count, rating distribution, top 10 authors/tags/series, by-language, 12-month read history; GET /api/stats returns per-user row (admins get every user + global "Tous utilisateurs"); Vue #/stats page with counters, monthly bar chart, rating distribution bars, top-N lists with links; stats button in header (single + multi-user); 2 new sqlite tests; v1.92.0**
- [x] Ajoute une fonction "ajout dans la pile à lire" qui constitue une liste ordonnée de livre à livre par utilisateur, il doit avoir une page pour consulter la liste et cela doit être aussi dans les flux OPDS1 et OPDS2. Une fois le livre indiquer lu on le retire de la liste. - **Done: catalog.ToReadManager + catalog.ToReadItem; SQLite migration v12 adds to_read_list (user_id, book_id, position, added_at); SetUserRead(true) auto-removes book from to-read list; API endpoints GET/POST/DELETE /api/to-read + PUT /api/to-read/reorder; OPDS v1 /opds/to-read + OPDS v2 /opds/v2/to-read acquisition feeds with per-user filtering; root nav entries on both feeds; Vue #/to-read page with numbered ordered list, up/down/remove buttons, badge counter in header (multi-user); "Ajouter à la pile à lire" / "Retirer de la pile" toggle button on book detail page; 3 sqlite tests; v1.93.0**
- [x] Rajoute la consultation et la manipulation de la pile à lire dans le mcp - **Done: 4 new MCP tools wired through catalog.ToReadManager — list_to_read (ordered list with title/author/series per item), add_to_read (append), remove_to_read, reorder_to_read (array of book_ids); s.toReadManager populated via type assertion in mcp.New(); all return French success/error text and respect "backend ne supporte pas" guard; 5 new tests in server_test.go; v1.94.0**
- [x] Met a jour le readme avec toute les dernieres modification - **Done: README features list adds to-read pile + reading statistics; MCP tool table extended with all current tools (add_wishlist_item, delete_wishlist_item, update_cover, list/add/remove/reorder_to_read, defaults clarified); OPDS feeds table adds /opds/to-read; REST API table adds /api/to-read endpoints + /api/stats**
- [x] Le token d'auth n'est pas bien propagé dans les flux OPDS pour la liste à lire j'ai une 401 - **Done: in multi-user mode, OPDS token-authenticated requests had no userID in context, so /opds/to-read and /opds/v2/to-read returned 401. Added `s.resolveUserForRequest(r)` helper (session-cookie userID → ?user=<id> query param → empty for single-user mode → 401 only when multi-user with no user identified). Root nav handlers now emit one "Pile à lire de <name>" entry per user when the request is token-authenticated in multi-user mode (each entry includes `?user=<id>&token=<tok>`); single entry in session-authenticated or single-user mode (existing behavior). 5 new tests cover single-user 200, multi-user 401 without ?user=, multi-user 200 with ?user=, multi-user 401 on unknown ?user=, and per-user nav entries in the root feed.**
- [x] Génére un token d'authentification OPDS et MCP par utilisateur afin que les recommandations et pile à lire et non lu soit celle de l'utilisateur - **Done: catalog.User gains Token field; UserManager extended with UserByToken + RegenerateUserToken; SQLite migration v13 adds users.token column with unique index and auto-generates 64-char hex tokens for existing users; CreateUser/RegenerateUserToken on sqlite backend wired in; authMiddleware accepts the per-user token on /opds/*, /covers/* and /mcp routes (in addition to the legacy shared OPDS token), injects userID into the request context on success; handleUnreadBooks/handleOPDS2Unread now pass userID into SearchQuery so the unread feed becomes per-user; handleOPDSRecommendations/handleOPDS2Recommendations route through new recommendedBooks(uid) helper that returns only the user's incoming recommendations when authenticated; handleOPDSToRead/v2 already routed through resolveUserForRequest so the per-user token immediately makes those feeds personal; /api/me + /api/users + /api/config + /api/users responses include the per-user token (admin only for the user list); POST /api/users/{id}/token regenerates a token (admin only); Vue admin UI shows masked token + Afficher / Copier URL OPDS / Régénérer per row; 6 new tests; v1.96.0**
- [x] Sur mobile le menu est trop long, il faudrait le passer en menu hamburger - **Done: all icon buttons in the header actions row hidden on mobile (`hidden sm:flex`); single hamburger button (`flex sm:hidden`) toggles `mobileMenuOpen`; dropdown is a sibling of the inner flex container (not clipped by `overflow-hidden`); shows user badge, update available, grid actions (téléverser/refresh/OPDS/copy URL), wishlist, to-read, recommendations, stats, admin, AI chat, dark mode toggle, logout — vertical full-width labelled rows; closes on every navigation via `onHashChange` and on any click inside the panel; hamburger hidden on reader view; v1.97.0**
- [x] Ajoute un filtre sur la taille des séries ou que ce soit un one shot - **Done: SeriesSize field on catalog.SearchQuery; seriesSizeClause in sqlite backend uses a series-count subquery (BETWEEN 2-3, 4-7, or >=8); fs backend pre-computes a series→count map and matchSeriesSize helper; ?series_size=standalone|short|medium|long on /api/books (and ids_only); 4 indigo toggle pills in Vue filter bar ("Tome unique", "Saga 2-3", "Saga 4-7", "Saga 8+") that select one at a time; gridFilterParams() includes the new param so prev/next book navigation stays in context; 2 new tests (fs + sqlite)**
- [x] Le filtre n'est pas visible en version bureau du site - **Done: root cause was a stale service-worker cache, not a layout bug. `handleSW` was injecting the version by replacing the literal string `"nxt-opds-v1"` but `sw.js` had been bumped to `"nxt-opds-v2"` in an earlier change, so the substitution silently failed and every release kept the same cache name (`nxt-opds-v2`). Clients therefore continued to serve the previously-cached `index.html` indefinitely — even after upgrading to v1.100.0 the new series-size filter pills were never delivered. Fix: `sw.js` now uses the literal token `__APP_VERSION__` (split with `'…-' + '__APP_VERSION__'` so the comment doesn't get rewritten too), and `handleSW` replaces that token with the running binary version. Each release now gets its own cache namespace; the existing `activate` handler deletes stale caches automatically. v1.101.0**
- [x] Possibilité de parametré des webhoks dans l'admin qui se declenche sur certaine action comme l'ajout d'un livre, ajoute dans le README le format json envoyé - **Done: catalog.Webhook + WebhookManager interface; SQLite migration v15 stores webhooks (id, name, url, events CSV, secret, enabled, created_at, last_fired_at, last_status); internal/webhooks package implements an async Dispatcher with HMAC-SHA256 X-NxtOpds-Signature header, 10s HTTP timeout, X-NxtOpds-Event header; events book.created (upload), book.updated (PATCH), book.deleted (DELETE) and book.read (toggle) fire after the mutation succeeds; admin-only /api/webhooks GET/POST + /api/webhooks/{id} PATCH/DELETE + /api/webhooks/{id}/test POST; Vue admin page gains a Webhooks section with create/edit modal, event multi-select, secret-preserving update flow, "Tester" button, last-status badge; 3 dispatcher tests; README updated with full JSON envelope reference; v1.102.0**
- [x] utilise plutot X-Signature pour le hearder hmac - **Done: webhook dispatcher header renamed from `X-NxtOpds-Signature` to the more conventional `X-Signature`; updated test, README, admin UI hint and Webhook struct doc-comments accordingly; v1.103.0**
- [x] Retirer l'IA embarquée (Ollama + Anthropic) de nxt-opds — **Done: deleted internal/ai/ package and internal/server/handlers_ai.go; removed OllamaURL/OllamaModel from config.go (yaml + OLLAMA_URL/OLLAMA_MODEL env handling) and server.Options; removed the AI init/log block from main.go; AIEnabled in /api/config now hardcoded to false (Vue chat UI auto-hides); removed POST /api/ai/chat route; Dockerfile.dev no longer installs Node/claude-code (kept curl+sqlite3 only); removed ANTHROPIC_API_KEY env line from nxt-opds.service; cleaned Ollama mentions from README features/env-vars/YAML/API/structure tables; v1.104.0**
- [x] Stocker l'association librarian côté nxt-opds en base — **Done: catalog.LibrarianAssociation interface with Get/Set/Clear + LibrarianAssociationData struct (LibrarianURL, LibrarianInstance, ChatSecret, WebhookSecret, CreatedAt, UpdatedAt); SQLite migration v16 creates singleton `librarian_association` table (id PK=1 CHECK constraint); sqlite Set() upserts via ON CONFLICT and preserves CreatedAt across replacements; fs backend persists to a separate `.librarian.json` file (0600 perms — secrets-on-disk) alongside .metadata.json, with the same CreatedAt-preserving Set semantics; no YAML config field (per-spec, pairing is UI-driven); 2 tests (sqlite singleton + idempotent Clear, fs persistence with 0600 perms); v1.105.0**
- [x] Endpoint génération de code d'appairage — **Done: internal/server/pairing.go implements pairingCodeStore with crypto/rand-backed Generate() (XXXX-XXXX over Crockford-Base32 alphabet `0123456789ABCDEFGHJKMNPQRSTVWXYZ`, 40 bits entropy, 10-min TTL) and single-use Consume() with case/dash normalisation and lazy expiry pruning; new requireSessionAdmin middleware in handlers_librarian.go that bypasses the per-user/OPDS-token codepath (only a real session cookie + IsAdmin=true gets through); POST /api/librarian/pairing-code returns {code, expiresAt: Unix ms}; pairingCodeStore wired into Server struct; LibrarianAssociation type-assertion wired through Server.librarianAssoc so the endpoint returns 501 when the backend can't persist a future pairing; Vue admin page gains a "Librarian" panel with "Associer un librarian" button → violet modal showing the 2xl-tracking code, a Copier button + live countdown timer; 9 tests (5 store-level, 4 endpoint covering admin happy path, non-admin 403, OPDS-token-only rejection, no-auth rejection); v1.106.0**
- [x] Endpoint d'appairage — **Done: POST /api/librarian/pair registered as a PUBLIC route (outside authMiddleware) since the one-time code in the body IS the authentication; new Validate() method on pairingCodeStore short-circuits obviously wrong codes before any DB I/O; handler flow = Validate(code) → existing-association check (409 unless force=true) → randomHexSecret(32) for chat + webhook (64-char hex each via crypto/rand) → librarianAssoc.Set() → Consume(code); response {mcp_url, mcp_token, chat_secret, webhook_secret, instance, label} where mcp_url is reconstructed from r.Host + X-Forwarded-Proto/Host headers (so reverse-proxy setups return the right public URL) and mcp_token is the existing OPDSToken; 503 when no OPDSToken configured (refuses to hand out an empty token), 501 when no LibrarianAssociation backend, 400 on missing fields/malformed JSON, 401 on invalid code, 409 on conflict; 7 new tests (happy path with persisted association + replay rejection, public-routability without auth middleware, four field-validation cases, conflict-without-force + force-replace, no-OPDS-token, X-Forwarded-* honoured); v1.107.0**
- [x] Endpoints association — **Done: GET /api/librarian/association returns 204 No Content when unpaired and {librarian_url, librarian_instance, created_at, updated_at, connected_at_last_ping} when paired — chat_secret and webhook_secret are never serialised (test asserts they don't appear in the body); DELETE clears the local row synchronously and fires a best-effort goroutine POST ${librarian_url}/instances/{instance}/forget with Authorization: Bearer ${chat_secret} (5 s timeout, errors logged only — local state is authoritative); X-Forwarded-* still honoured via externalURL helper; both endpoints protected by requireSessionAdmin so a leaked OPDS token can't enumerate or break the pairing; librarianForgetClient global is a swappable HTTP client (captured at request time so tests can `defer` restore without racing the goroutine); Vue admin "Librarian" panel auto-loads the association via loadLibrarianAssociation() on hash change, shows a green-dot "Librarian appairé" card with URL/instance/created_at when paired and a red "Désappairer" button (with confirm dialog) or falls back to the "Associer un librarian" button + intro text when unpaired; closePairingDialog() refreshes the card so a freshly completed librarian handshake flips the view immediately; 6 new tests (GET 204 empty, GET 200 + safe-fields assertion that secrets don't leak, GET non-admin 403, DELETE clears+notifies remote with captured Bearer/path, DELETE idempotent when absent, DELETE succeeds despite remote network failure); v1.108.0**
- [x] Endpoint rotation — **Done: POST /api/librarian/rotate registered as a PUBLIC route (the librarian holds no session cookie); shared chatSecretHeader constant ("X-Librarian-Chat-Secret") factored out so the upcoming /forget endpoint can reuse it; new authenticateLibrarian() helper short-circuits 501 (no backend), 404 (no association), 401 (missing header) and 401 (wrong header) via crypto/subtle.ConstantTimeCompare, returning the matched association on success; handler then mints two fresh 32-byte hex secrets via randomHexSecret(32) and calls librarianAssoc.Set() with the same URL/instance so CreatedAt is preserved; response {chat_secret, webhook_secret, instance}; 5 tests cover happy path with full assertion that CreatedAt sticks while UpdatedAt advances and that the old secret stops working after rotation, missing-header 401, wrong-header 401 + association untouched, no-association 404, and reachability without any session/OPDS auth (header is the only credential); v1.109.0**
- [x] Brancher le dispatch webhook automatique vers le librarian — **Done: new LibrarianTargetProvider interface in internal/webhooks (single Get() method, structurally satisfied by any catalog.LibrarianAssociation backend); Dispatcher.SetLibrarianTarget(p) wires it; Fire() now fans out in parallel to admin-configured hooks AND (when a target is set and Get() returns a non-empty association with WebhookSecret) to `${librarian_url}/webhooks/${librarian_instance}/book-event` via new deliverLibrarian() — same envelope, X-NxtOpds-Event header, X-Signature: sha256=HMAC-SHA256(webhook_secret, body), 10s timeout via the existing client, no RecordWebhookFire call (parallel target, not in webhooks table); FireOne is unchanged (test-button bypasses the librarian on purpose); pair/unpair takes effect immediately since the target is re-queried on every Fire(); short-circuit when mgr=nil AND target=nil avoids the unnecessary marshal; 5 new tests (body+sig+path+method assertion against the expected HMAC, no-association skip, missing-webhook-secret skip, parallel-with-admin firing both targets exactly once and only the admin one is recorded, no-target-no-op); v1.110.0**
- [x] Remplacer POST /api/ai/chat par un relais SSE vers le librarian — **Done: POST /api/ai/chat re-registered under `protected` (any logged-in user, not admin-only — chat is a user feature); handleAPILibrarianChat reads librarianAssoc and returns 404 when unpaired (so the SPA can hide the chat box); upstream request is built with http.NewRequestWithContext(r.Context(), …) so a client disconnect cancels the upstream call (no total timeout — chat is long-lived); inbound r.Body is streamed as-is to ${librarian_url}/chat with Authorization: Bearer ${chat_secret} + Accept: text/event-stream + the inbound Content-Type; response headers Content-Type (defaulting to text/event-stream), Cache-Control: no-cache, Connection: keep-alive, X-Accel-Buffering: no (disables nginx buffering); upstream status is mirrored; body is forwarded chunk-by-chunk via a 4 KiB buffer with http.Flusher.Flush after each chunk so events surface in real time; transport-level failures surface as 502 Bad Gateway; librarianChatClient is a swappable global so tests can inject synthetic failures; 4 new tests cover no-association 404, full relay flow (upstream method/path/Authorization/body + relayed Content-Type/X-Accel-Buffering + multi-frame body forwarded), transport failure → 502, upstream non-2xx status mirrored verbatim; v1.111.0**
- [x] Faire évoluer la chat box Vue pour consommer le SSE — **Done: chatMessages model changed from `{role, content}` to `{role, blocks:[]}` with block types `text`, `tool_call` (collapsable violet box with cog icon + name + pretty-printed JSON input) and `tool_result` (collapsable emerald box with checkmark + name + pretty-printed output); template falls back to `msg.content` for backwards compat; new parseSSEStream() reader uses `response.body.getReader()` + TextDecoder with `{stream:true}`, CRLF→LF normalisation and a persistent buffer so an event boundary can fall mid-chunk; per-frame onFrame callback dispatches on `text` (appended to / starts a new text block), `tool_call` (parsed JSON → new block, picks up `name`+`input` or falls back to the raw payload), `tool_result` (same, supports `output`/`result` shapes), `error` (renders as text then 'stop'), `done` ('stop'), and unknown events (rendered as text rather than silently dropped); chatLoading dismissed on the first received frame so the bouncing dots fade out as streaming begins; auto-scroll on every block; request body is now `{message, history}` (matches the librarian /chat contract from task #69); 404 from the relay shows a friendly "Configure-le dans l'administration" message instead of erroring; formatToolPayload helper pretty-prints JSON or returns strings verbatim; v1.112.0**
- [x] aiEnabled → librarianEnabled dans GET /api/config — **Done: configJSON field AIEnabled → LibrarianEnabled (json:"librarianEnabled"); computed once per request via librarianAssoc.Get() — true only when a row exists in the catalog backend with a non-empty LibrarianURL (so the SPA hides the chat box until a real handshake has happened, not merely when the backend supports the interface); legacy `aiEnabled` key is gone from the response (test asserts the JSON string doesn't contain `"aiEnabled"`); Vue ref renamed `aiEnabled` → `librarianEnabled` and all 5 references updated in lock-step (header desktop chat button, mobile-menu chat button, floating chat panel, /api/config read, return block); 2 new tests pin the new contract (false on a fresh server + assertion that the legacy key is gone, true after backend.Set seeds an association); v1.113.0**
- [x] Endpoint /api/librarian/forget (entrant) — **Done: POST /api/librarian/forget registered as a PUBLIC route (librarian holds no session cookie); auth by X-Librarian-Chat-Secret header with crypto/subtle.ConstantTimeCompare; flow = always require the header (refuses to act as a "is anyone paired?" oracle), read association → if absent return 204 (idempotent — librarian's retry on a previously-handled unpair is a no-op), if present and header matches → Clear() → 204, mismatch → 401; 501 when backend has no LibrarianAssociation impl; 5 tests cover happy path (clears the row, 204), idempotent-when-already-gone (header still required, returns 204), wrong-secret 401 with association preserved, missing-header 401, and public-routability without session/OPDS auth; v1.114.0**
- [x] Mettre à jour le README — **Done: added a new "Librarian" section between MCP Server and API Endpoints with an ASCII topology diagram, the 4-step pairing flow (admin code → `librarian pair --nxt-opds … --code …` → secret exchange → SPA flips librarianEnabled), and a 7-row endpoint table (pairing-code, pair, GET/DELETE association, rotate, forget, /api/ai/chat) annotated with the auth requirement for each row; Features bullet replaced the old AI line with "Librarian pairing — pair an external 'librarian' service to enable an SSE-streaming chat assistant"; REST API table extended with the 7 librarian endpoints + /api/ai/chat; Webhooks section gained a "Librarian fan-out" subsection explaining the parallel target at `${librarian_url}/webhooks/${instance}/book-event` (not editable from the admin UI, removed only by unpairing); confirmed no remaining Ollama/Anthropic mentions; v1.115.0**
- [x] Endpoint POST /api/librarian/announce — **Done: POST /api/librarian/announce registered as a PUBLIC route (librarian holds no session cookie); reuses authenticateLibrarian (501 no backend / 404 no association / 401 missing-or-wrong X-Librarian-Chat-Secret); body `{"librarian_url":"http://..."}` is trimmed of trailing slash + whitespace and parsed via net/url — requires an absolute http:// or https:// URL with a Host (rejects "ftp://…", "/relative", "no.scheme:9090", "%-malformed", missing/empty field, invalid JSON all with 400); on success calls librarianAssoc.Set() with the new URL but the existing instance + both secrets → CreatedAt is preserved automatically by the sqlite backend, only UpdatedAt advances; returns 204 No Content; idempotent (same URL twice is a no-op); 9 new tests cover happy path with all-fields-preserved assertion, trailing-slash trim, idempotency, missing-header 401, wrong-header 401, no-association 404, 8 malformed-URL 400 sub-cases, and reachability without session/OPDS auth. README updated. v1.116.0**
- [x] Endpoint POST /api/librarian/heartbeat — **Done: catalog.LibrarianAssociation gains a Touch(time.Time) method; LibrarianAssociationData gains a LastSeenAt field; SQLite migration v17 adds a `last_seen_at INTEGER NOT NULL DEFAULT 0` column to the singleton table; fs backend persists `lastSeenAt` (Unix seconds) in `.librarian.json`; both Set() implementations preserve LastSeenAt across mutations (rotate/announce) — only Touch() advances it. POST /api/librarian/heartbeat reuses `authenticateLibrarian` (501 no backend / 404 no association / 401 missing-or-wrong X-Librarian-Chat-Secret); accepts optional `{"version":"vX.Y"}` body (parsed-and-discarded; 400 on malformed JSON); stamps LastSeenAt via Touch(time.Now()) without advancing UpdatedAt; returns 204. GET /api/librarian/association replaces `connected_at_last_ping` with `last_seen_at` (Unix ms, 0 when no heartbeat). Vue admin Librarian card swaps the static green dot for a coloured one driven by `librarianHeartbeatColor()` (green < 3 min, amber 3–15 min, red > 15 min, grey when no heartbeat) plus a relative "· connecté il y a Ns" label that refreshes every 15 s via a `setInterval`-backed ref. 9 new tests (8 heartbeat-endpoint variants — happy path with LastSeenAt set and UpdatedAt unchanged, empty body, idempotency, missing-header 401, wrong-header 401 + LastSeenAt unchanged, no-association 404, public-routability without session/OPDS auth, malformed-JSON 400 — plus a GET /association test that asserts last_seen_at surfaces as Unix milliseconds after Touch); the existing ReturnsSafeFields test was updated to assert LastSeenAt == 0 pre-heartbeat and that the legacy `connected_at_last_ping` key is no longer present in the response. README updated. v1.117.0**

- [x] Chat box JSON au lieu de SSE — **Done: removed `parseSSEStream`, `safeParseJSON`, `pushAssistantText` and `formatToolPayload` from web/index.html; `sendChatMessage()` now sends `Accept: application/json`, awaits `res.json()` and pushes a single `{role:"assistant", blocks:[{type:"text", text: data.reply}]}` message (or `Erreur : ${data.error}` when the upstream returns an error field); the loading dot-indicator stays visible until the fetch resolves, then is dismissed in the `finally`. Template no longer renders `tool_call`/`tool_result` blocks (only `text` blocks + `msg.content` fallback for legacy messages, format remains compatible). 404 → "Aucun librarian appairé"; non-2xx → "Erreur : HTTP …"; invalid JSON → "Erreur : réponse invalide du librarian". `/api/ai/chat` Go relay unchanged (it forwards bytes verbatim and mirrors Content-Type upstream). README "Topology" diagram + features bullet + endpoint tables updated to remove SSE references. v1.118.0**

- [x] Indicateur de réflexion chatbot — **Done: thinking bubble keeps the 3 bouncing dots but now adds an italic label next to them. Initial label « Le bibliothécaire consulte le catalogue… »; after 8 s a rotation kicks in (4 phrases — « Toujours en train de chercher… », « Croisement des métadonnées en cours… », « Encore quelques secondes… », « Analyse de votre bibliothèque… ») cycling every 7 s. An « Annuler » underline button on the right calls `AbortController.abort()` which cancels the in-flight fetch and restores the original message into `chatInput` (focus follows). A 60 s timer set at request start fires `chatTimedOut=true` then aborts; when the spinner closes, an amber banner appears with « La requête a pris trop de temps » and a « Réessayer » button that re-sends the same message. `sendChatMessage()` stores the message in `chatRetryMessage` at the very start and clears it on success/404 so retry only re-uses unfinished messages. `focusChatInput()` and `scrollChatToEnd()` are called in `finally` to preserve UX. Aborts (manual or timeout) don't push an assistant message — the timeout banner is the only feedback. v1.119.0**
- [x] Chat relay : forward le token OPDS de l'utilisateur connecté — **Done: `handleAPILibrarianChat` now buffers the inbound body, resolves the connected user via a new `userTokenForChat(r)` helper (1) session-cookie userID → catalog.UserManager.UserByID().Token, (2) anonymous request + exactly one user registered → that user's token, (3) returns "" otherwise so the librarian falls back to its instance token), JSON-decodes the body into `map[string]any`, sets `payload["user_token"] = token`, re-encodes and forwards as `bytes.NewReader(outboundBody)`. Empty Body or non-object JSON keeps the original body verbatim. README "Topology" section updated to document the injection + which MCP calls it scopes. 3 new tests (`InjectsUserTokenForSession` asserts Alice's token surfaces upstream, `NoUserTokenWhenAnonymous` asserts the body is forwarded verbatim and contains no `user_token` substring in multi-user + no-session mode, `SingleUserFallback` asserts the lone registered user's token is injected automatically). v1.120.0**
- [x] Notation « piment » 0-5 sur les livres 16+ et 18+ — **Done: `SpiceRating int` field added to `catalog.Book` (0 = aucun/non noté, 1 = suggestif, 2 = romance sensuelle, 3 = scènes explicites occasionnelles, 4 = explicite récurrent, 5 = très explicite/érotique); SQLite migration v18 adds `spice_rating INTEGER NOT NULL DEFAULT 0` to the books table (defensive ALTER tolerates already-present column); fs backend persists via metaOverride `.metadata.json` (`spiceRating` JSON key, merge-on-load); PATCH /api/books/{id} accepts `spiceRating` and validates 0-5 range (400 on out-of-range); `bookJSON` API exposes the field; webhook envelope includes `spiceRating`; MCP `update_book` tool gains `spice_rating` (numericArg, clamped 0-5); MCP version bumped 1.121.0 → 1.122.0; `formatBookDetail` shows « Intensité (piment): N/5 » only when AgeRating >= 16 AND SpiceRating > 0. `catalog.SearchQuery.SpiceMax *int` filter: both backends apply « age_rating < 16 OR spice <= max » so child/teen books are exempt; `?spice_max=N` query param on /api/books accepts 0-5 (clamped). UI: edit form shows a 🌶 0-5 selector ONLY when `ageRating >= 16`, defaults to 0 and is force-reset to 0 when saving with age < 16 so accidental selections don't persist; book detail page renders a coloured « 🌶 N/5 » badge (gradient: green→yellow→amber→orange→red as intensity rises) next to the age pastille when SpiceRating > 0; filter bar has 5 red toggle pills (🌶 0, ≤1, ≤2, ≤3, ≤4) that are hidden for child profiles (`currentUser.isChild`); spiceMaxFilter persists in localStorage as `nxt-spice-max`; `gridFilterParams` includes spice_max so prev/next book navigation keeps context. Also fixed a stale-pointer bug in `fs.Backend.StoreBook` that surfaced while writing the test: prepending to `b.books` reallocates the slice so previously cached `b.byID` pointers became stale — now the whole index is rebuilt after each upload. 4 new tests cover: fs persistence + filter, sqlite persistence + filter, migration v18 column presence, handler validation (200 with 4, 400 with 7/-1), handler ?spice_max filter (excludes Spicy/spice 4, keeps Mild/spice 1 and Child/under 16). README updated (features bullet + book.created/book.updated JSON envelope). v1.122.0**

- [x] Filtre piment dans les flux OPDS — **Done: new `parseSpiceMaxQuery(r)` helper in handlers.go (clamps 0-5, returns *int or nil) replaces the inline parsing in handleAPIBooks; every OPDS handler that returns a book list now applies it alongside `MaxAgeRating: s.maxAgeRatingForUser(userID)` so child profiles are unaffected and external OPDS readers can restrict spicy content the same way the web UI does. Handlers updated: handleAllBooks (switched from AllBooks() → Search() so the filters compose), handleUnreadBooks, handleSearch, handleAuthorBooks/handleTagBooks/handlePublisherBooks (switched to Search() only when spice_max OR maxAge is in effect, otherwise keep the existing BooksByX fast path), and their OPDS v2 counterparts (handleOPDS2Publications/Unread/Search/AuthorBooks/TagBooks/PublisherBooks). New `spiceLevels` const + `handleSpiceLevels` (OPDS v1 navigation feed listing 5 buckets at /opds/spice → /opds/books?spice_max=N) + `handleOPDS2SpiceLevels` (OPDS v2 at /opds/v2/spice → /opds/v2/publications?spice_max=N); both return an empty feed for child profiles. Root feeds (handleRoot + handleOPDS2Root) advertise a "Niveaux de piment" entry only when `maxAgeRatingForUser(uid) == 0` so child profiles never see it. Routes registered in server.go. 6 new tests cover: root v1 entry present, root v2 nav item present, /opds/spice → 5 entries with `/opds/books?spice_max=0..4`, /opds/v2/spice → 5 nav items with `/opds/v2/publications?spice_max=0..4`, GET /opds/books?spice_max=2 excludes Spicy(18+/4), keeps Mild(16+/1) + Child(6+), and HiddenForChildProfile asserts both the root entry and the /opds/spice list disappear for a `IsChild=true MaxAge=6` user. v1.124.0**

- [x] Badge piment masqué quand age_rating < 16 — **Done: book-detail container `v-if` now `currentBook.ageRating > 0 || currentBook.spiceRating > 0` and the inner spice `<span>` only checks `currentBook.spiceRating > 0` (the age pastille is wrapped in its own `<template v-if="ageRating > 0">` so it disappears cleanly when only spice is set). Edit form spice selector is now always rendered with a small grey hint `« Plus pertinent sur les livres 16+/18+ »`; when the user picks spice ≥ 3 on a sub-16+ book an amber sub-line surfaces `« Conseil : classer ce livre en 16+ ou 18+ »` (non-blocking — saving still works). `saveEdits()` no longer force-resets spiceRating to 0 when ageRating < 16, so the value the user just entered actually persists. Grid card cover gains an absolute-positioned top-right pill `🌶 N` styled with `spiceBadgeClass(book.spiceRating)` and a hover title `« Piment : <label> »` whenever `book.spiceRating > 0`; the pill uses tiny text (text-[10px]) so it doesn't overlap the cover artwork. v1.125.0**
- [x] Filtre piment passe en valeur EXACTE — **Done: `SpiceExact *int` était déjà en place côté backends + handlers OPDS (depuis v1.122/v1.126) ; cette release finalise le passage exact côté API publique et UI. Concrètement : `handleAPIBooks` lit désormais `?spice=N` via `parseSpiceExactQuery` et le propage dans les deux branches (`ids_only=1` et pagination normale) — la propagation manquait, le filtre exact n'était donc pas appliqué au flux `/api/books`. La table `spiceLevels` passe de 5 à 6 entrées (0 à 5) avec libellés courts (« Sans »/« Suggestif »/« Sensuel »/« Explicite »/« Récurrent »/« Érotique »), et les liens des feeds OPDS pointent vers `?spice=N` au lieu de `?spice_max=N` (handlers v1 et v2). MCP `search_books` gagne le paramètre `spice_rating` (int exact 0-5, clampé) à côté de `spice_min`/`spice_max` qui restent pour les plages avancées ; `serverInfo.version` 1.126.0 → 1.127.0. UI Vue : les 5 pills « 🌶 0/≤1/≤2/≤3/≤4 » deviennent 6 pills exactes « 🌶 0 Sans » → « 🌶 5 Érotique » ; nouvel état `spiceExactFilter` persistant dans localStorage sous `nxt-spice` ; au boot la clé legacy `nxt-spice-max` est explicitement nettoyée sans conversion (un seuil n'est pas équivalent à un match exact). `gridFilterParams()` envoie `?spice=N` pour que la navigation prev/next garde le contexte. Le paramètre `?spice_max=` reste accepté côté serveur pendant une release pour les clients OPDS qui auraient mis ses URLs en favoris. Tests : `TestToolSearchBooks_SpiceExact` vérifie qu'avec `spice_rating:4` seul "Adulte épicé" sort (les sub-16 sont exclus malgré spice=0) ; `TestHandleAPIBooks_SpiceExactFilter` vérifie qu'`?spice=4` ne renvoie que le livre 18+/spice=4 et exclut les autres adultes et les enfants ; les nav-feeds tests `TestHandleSpiceLevels_SixEntries` / `TestHandleOPDS2SpiceLevels_SixItems` valident 6 entrées + hrefs `?spice=N` ; les tests `SpiceMax` existants restent verts (rétro-compat). v1.127.0**

- [x] Afficher la date de lecture (Lu le …) sur la page d'un livre — **Done: nouveau interface optionnel `catalog.UserReadAtProvider` avec une seule méthode `UserReadAt(userID, bookID) (time.Time, error)` qui retourne la zero-value sans erreur quand le livre n'est pas marqué lu ou que `read_at = 0` (lignes pré-migration v11). Le backend SQLite implémente l'interface en lisant la colonne `read_at` déjà existante (la migration v11 et le stamp dans SetUserRead étaient déjà en place pour les statistiques — aucune migration de schéma nouvelle). Le handler `handleAPIBook` (et `handleAPIUpdateBook`) lit `s.userReadManager.(catalog.UserReadAtProvider)` via type-assertion, l'appelle uniquement quand `isRead=true && userID != ""`, et expose `readAt` (Unix ms, `omitempty`) sur `bookJSON`. La page détail Vue ajoute une ligne « Lu le DD mois YYYY » colorée verte (sous « Indexé le ») rendue uniquement quand `currentBook.isRead && currentBook.readAt > 0` — `formatReadDate(d)` utilise `toLocaleDateString('fr-FR', {day:'2-digit', month:'long', year:'numeric'})` pour un format lisible (« 12 mai 2026 »). Le PATCH /api/books/{id} renvoie aussi `readAt` pour que le bandeau s'affiche immédiatement après un clic sur « Marquer comme lu » sans rechargement. 1 nouveau test `TestUserReadAt_RecordedAndReturned` (3 cas : unread → zero, après SetUserRead → timestamp dans la fenêtre, arguments vides → zero+nil). v1.128.0**

- [x] Retirer définitivement le paramètre `?spice_max=` côté API publique et OPDS — **Done: la fonction `parseSpiceMaxQuery` et tous ses appels HTTP supprimés de `handlers.go` (13 sites : handleUnreadBooks, handleAllBooks, handleSearch, handleAuthorBooks, handleTagBooks, handlePublisherBooks, handleAPIBooks dans ses deux branches `ids_only`/paginée, handleOPDS2Unread, handleOPDS2Publications, handleOPDS2Search, handleOPDS2AuthorBooks, handleOPDS2TagBooks, handleOPDS2PublisherBooks). Les hoists `spiceMax := …` et leurs conditions `if spiceMax != nil || …` sont également retirés. `SpiceMax *int` reste sur `catalog.SearchQuery` parce que MCP `search_books(spice_max:N)` continue à l'exposer comme filtre avancé documenté. Les deux OPDS v2 handlers (Publications et Search) qui avaient `SpiceMax: parseSpiceMaxQuery(r)` mais pas de `SpiceExact:` (oubli du précédent loop) gagnent désormais `SpiceExact: parseSpiceExactQuery(r)` à la place — symétrie complète avec l'OPDS v1. Doc-comment `// Honours ?spice_max=N` corrigé en `?spice=N` (exact). Tests : `TestHandleAPIBooks_SpiceMaxFilter` → réécrit en `TestHandleAPIBooks_SpiceMaxIgnored` qui assure que `?spice_max=2` est désormais un no-op (Spicy Book reste visible) ; `TestHandleAllBooks_OPDS_SpiceMaxFilter` → réécrit en `TestHandleAllBooks_OPDS_SpiceExactFilter` qui valide `?spice=4` côté /opds/books ; tous les autres tests passent sans modification. README mis à jour pour annoncer le retrait. MCP `serverInfo.version` 1.127.0 → 1.129.0 (saute 1.128 qui était la release "show Lu le date" sans modif MCP). v1.129.0**

- [x] MCP search_books : exposer les filtres age_rating et spice_max — **Done: new `SpiceMin *int` field added to `catalog.SearchQuery` (mirrors `SpiceMax` semantics; sub-16+ books are excluded when `SpiceMin > 0` because their spice is undefined). Both backends honour it: sqlite appends `(b.age_rating >= 16 AND COALESCE(b.spice_rating, 0) >= ?)` next to the existing SpiceMax clause; the fs backend's Search loop drops books where `bk.AgeRating < 16 || bk.SpiceRating < v`. MCP `search_books` schema gains 4 new args: `age_rating` (exact integer 3/6/10/12/16/18, -1=unclassified — maps to AgeRatings=[v]), `age_rating_min` (3/6/10/12/16/18 — expands to all paliers ≥ N, so 16 returns 16+ AND 18+ in one call — overrides `age_rating` when both supplied), `spice_min` (0-5, clamped, only applied when >0 — preserves "no filter" behaviour on 0), and `spice_max` (existing SpiceMax). Handler uses `numericArg` so JSON numbers and string-encoded integers both work. MCP `serverInfo.version` bumped 1.125.0 → 1.126.0. Fake catalog Search() updated to honour AgeRatings/SpiceMin/SpiceMax for tests. 4 new tests: `TestToolSearchBooks_AgeRatingExact` (age_rating=18 → only 18+), `TestToolSearchBooks_AgeRatingMin` (age_rating_min=16 → 16+ AND 18+), `TestToolSearchBooks_SpiceMin` (spice_min=3 → only Adulte épicé spice=4), `TestToolSearchBooks_SpiceMaxWithAgeMin` (combine age_rating_min=16 + spice_max=2 → only 16+/18+ doux). README MCP table line updated to list the new filters. v1.126.0**

- [x] Refonte de la barre de filtres : drawer + chips actifs — **Done: la barre est devenue 3 lignes (au lieu d'une seule scrollable saturée). Row 1 « pinned essentials » : compteur de résultats à gauche, lien « Étiquettes » (≥sm uniquement), select de tri, bouton « Filtres » avec badge `{{activeFilterCount}}` à droite. Row 2 « chips actifs » (rendue uniquement quand `activeFilterCount > 0`) : un badge supprimable par filtre actif (« Non lus seulement », « Non indexés », « Âge : 16+ » par valeur sélectionnée — un chip distinct par âge car `ageRatingFilter` est multi-select, « Saga : Tome unique », « 🌶 2 Romance sensuelle ») + lien underline « Tout effacer ». Le panneau de filtres est rendu en bottom-sheet plein-largeur sur mobile (`items-end`, `rounded-t-2xl`) / popover ancré à droite sur desktop (`sm:items-start sm:justify-end sm:w-[28rem] sm:rounded-xl`), backdrop noir semi-opaque cliquable pour fermer (`@click.self`), max-h 88vh (mobile) / 80vh (desktop) avec body scrollable et header+footer sticky. Sections : **Lecture** (grid 2 colonnes : « Non lus seulement », « Non indexés »), **Classification d'âge** (7 pills filtrées par `ageRatingsForUser` qui retire 16+/18+ pour les profils enfant en fonction de `currentUser.maxAge`), **Avancé** (header cliquable avec chevron rotatif qui toggle `advancedExpanded` — état persisté dans localStorage `nxt-filters-advanced` — contient Taille de série 4 pills indigo + Piment 6 pills rouges exactes 🌶 0..5 entièrement masquées si profil enfant + lien « Gérer les étiquettes » mobile-only). Footer sticky : « Tout effacer » (ghost, désactivé si aucun filtre) à gauche + « Voir les résultats » (primary brand) à droite. Nouvelles refs `filtersOpen=false` et `advancedExpanded` (persisté). Tables `seriesSizes` et `_ageRatings` + helper `seriesSizeLabel(v)`. Computeds `ageRatingsForUser`, `activeFilterCount` (unread + notIndexed + ageRatingFilter.length + seriesSize + spice≥0), `activeFilterChips` (un chip par valeur d'âge pour suppression individuelle, key `age:<v>`). Fonctions `toggleAdvancedExpanded`, `removeFilter(key)` (switch sur key incluant `age:<v>` parsé), `clearAllFilters` (reset tous les filtres mais préserve searchQuery et sortOrder). `onKeyDown` gagne un short-circuit Escape qui ferme `filtersOpen` avant le code arrow-key existant — fonctionne sur toute vue. Tous les anciens pills inline + la rangée scrollable `pills-scroll` retirés du markup. MCP `serverInfo.version` 1.129.0 → 1.130.0. v1.130.0**

- [x] OPDS v2 : conformité à la spec https://drafts.opds.io/opds-2.0 + alignement sur le catalogue de référence — **Done: `internal/opds2/feed.go` — `Feed.Navigation` passe de `[]NavItem` à `[]Link` (Compact Collection de Link Objects, spec OPDS 2.0 §2.4). Le type `NavItem` est supprimé. `internal/server/handlers.go` — 11 sites de construction de nav items réécrits en `opds2.Link{...}` sans champ `Rel` (alignement test.opds.io/2.0/home.json) : `handleOPDS2Root` (5 entrées de base + Niveaux de piment + Liste de souhaits + Recommandations), `appendToReadV2NavItems` (Pile à lire + Pile à lire par user en multi-user), `handleOPDS2SpiceLevels` (6 niveaux), `handleOPDS2Wishlist`, `handleOPDS2Authors`, `handleOPDS2Tags`, `handleOPDS2Publishers`. Le `Rel: "subsection"` qui pré-existait sur authors/tags/publishers est aussi retiré pour rester fidèle à la référence. Les `links[]` (self/start/search) gardent leurs rels — seuls les `navigation[]` sont concernés par la suppression. Test existant `TestHandleOPDS2Root_HasSpiceNavItem` passe (type `*opds2.NavItem` remplacé par `*opds2.Link`). Nouveau test `TestHandleOPDS2Root_NavigationCompactCollection` qui assert (a) le body brut ne contient PAS `"current"`, (b) chaque navigation[i] a `Rel == nil` + title + href non vides, (c) links[] contient bien self/start/search avec `templated: true` sur search. MCP `serverInfo.version` 1.130.0 → 1.131.0. v1.131.0**

  **Diffs observées vs. la référence test.opds.io/2.0/home.json** :
  - **Navigation items SANS `rel`** dans la référence. Mon code émet `rel:"current"` partout → drop le rel sur tous les navigation items du root, sauf cas spec (`rel:"http://opds-spec.org/sort/new"` etc.) qui n'est pas mon usage. Deux causes documentées par la spec OPDS 2.0 :

  **1. `rel: "current"` sur des entrées qui pointent vers des sous-catalogues.** La spec définit `current` comme « the destination of the link contains the current view of the resource described by the link's context » — sémantiquement faux pour « Tous les livres », « Par auteur », « Par genre », etc. qui sont des sous-feeds à parcourir. Le rel correct est `subsection`. Certains readers stricts ignorent ou rejettent les nav items dont le rel n'est pas dans leur liste blanche. Remplacer `Rel: "current"` par `Rel: "subsection"` dans `handleOPDS2Root` (`internal/server/handlers.go:3001-3030`) ET dans `appendToReadV2NavItems` (lignes 3041-3060) ET dans `handleOPDS2SpiceLevels` (~660-680), `handleOPDS2Authors` / `handleOPDS2Tags` / `handleOPDS2Publishers` (les nav items qu'ils émettent vers les sous-feeds par auteur/tag/éditeur ont déjà `rel:"subsection"` — vérifier que c'est cohérent).

  **2. Compact-collection purity.** La spec : « A navigation collection ... It must be a compact collection. » Un compact collection est strictement `[]Link Object`. Mon Go type `opds2.NavItem` (`internal/opds2/feed.go:38-44`) sérialise en `{title, href, type, rel}` — c'est le shape exact d'un Link Object minus `templated`, donc fonctionnellement OK. Mais pour suivre la spec à la lettre et rester robuste, supprimer `NavItem` et utiliser `[]Link` directement dans `Feed.Navigation`. Le `Templated bool` reste `omitempty`, donc pas de pollution de l'output.

  **3. Bonus :** vérifier que `Content-Type` reste exactement `application/opds+json; charset=utf-8` (déjà OK), et que toutes les hrefs `templated:true` ont effectivement le marqueur (la search link de la racine l'a).

  Tests : ajouter un test sur la racine `/opds/v2` qui assert (a) **les navigation items n'ont PAS de rel** (alignement test.opds.io), (b) absence de `rel:"current"` dans tout le doc, (c) `links[].rel` correctement formé sur `self`/`start`/`search`. Tester en réel avec **Cantook (Android ou iOS)** — c'est le client cible — et Foliate (gtk) en backup. v1.131.0+.

- [x] OPDS v2 : exposer les filtres âge & piment en `facets` plutôt qu'en sous-feeds — **Done: `internal/opds2/feed.go` — ajout des types `FacetGroup`, `FacetMetadata`, `LinkProperties` ; ajout du champ `Facets []FacetGroup` au `Feed` (`json:"facets,omitempty"`) et du champ `Properties *LinkProperties` au `Link`. `internal/catalog/catalog.go` — nouveau type `FacetCounts{AgeRating, Spice map[int]int}` + nouvelle interface optionnelle `Faceter{Facets(SearchQuery) (FacetCounts, error)}`. `internal/backend/fs/fs.go` — implémentation `Facets()` en une passe : applique tous les filtres de la query SAUF AgeRatings (pour le bucket âge) et SAUF Spice (pour le bucket piment) ; MaxAgeRating (profil enfant) reste appliqué donc la map AgeRating ne contient que des buckets ≤ MaxAge et la map Spice reste vide. `internal/backend/sqlite/sqlite.go` — refactor : extraction du WHERE-clause-builder dans `buildSearchFilters(q, opts)` avec opts `skipAgeRatings`/`skipSpice` ; `Search()` utilise opts vide ; `Facets()` lance 2 GROUP BY (`SELECT age_rating, COUNT(*) ... GROUP BY age_rating` + idem spice scope age ≥ 16) avec le bon opts. Compatible FTS5 et fallback LIKE. `internal/server/handlers.go` — nouvelles helpers `ageFacetLabel(v)` (« Non classifié », « 3+ », « 6+ »...), `spiceFacetLabel(v)` (réutilise les libellés `spiceLevels`), `buildFacetURL(basePath, cur, param, value, tok)` qui rebuild une URL en préservant la querystring sauf offset/limit/token/param-courant, et la méthode `Server.buildFacetGroups(r, q, basePath, tok)` qui type-asserte le backend en `faceter`, construit le groupe « Classification d'âge » (skip les buckets vides + skip ceux > MaxAgeRating pour les enfants) et le groupe « Piment » (omis pour les profils enfant ET quand aucun livre 16+/18+ ne match). Wiring sur `handleOPDS2Publications` (avec parsing `?age_rating=`), `handleOPDS2Unread`, `handleOPDS2AuthorBooks`, `handleOPDS2TagBooks`, `handleOPDS2PublisherBooks` — chaque handler bascule sur `Search(q)` uniformément (le fast-path BooksByAuthor/Tag/Publisher est remplacé) pour que les facets composent avec les filtres déjà actifs. 4 nouveaux tests : `TestHandleOPDS2Publications_FacetsAge` (facet âge présent avec NumberOfItems sur 3+/6+/12+/18+, facet piment présent quand un livre 18+/spice=3 existe), `TestHandleOPDS2Publications_FacetsHiddenForChild` (profil enfant MaxAge=10 → pas de facet Piment, facet Âge sans buckets > 10), `TestHandleOPDS2TagBooks_FacetsPreserveTag` (facets sur /opds/v2/tags/Fantasy → hrefs commencent par /opds/v2/tags/Fantasy, counts scope à la balise), `TestHandleOPDS2Publications_FacetCountMatchesSearch` (round-trip : suivre l'href du facet 6+/count=2 renvoie bien 2 publications). MCP `serverInfo.version` 1.131.0 → 1.132.0. v1.132.0** **Cantook rend les facets en chips de filtre cliquables en haut de la liste de publications** — ce rendu est l'UX cible. Cf. spec §2.6 et l'exemple :

  ```json
  "facets": [
    { "metadata": { "title": "Language" },
      "links": [
        { "href": "/fr", "type": "application/opds+json", "title": "French", "properties": { "numberOfItems": 10 } },
        { "href": "/en", "type": "application/opds+json", "title": "English", "properties": { "numberOfItems": 40 } }
      ]
    }
  ]
  ```

  **Types Go (`internal/opds2/feed.go`)** :
  ```go
  type Feed struct {
      Metadata     FeedMetadata `json:"metadata"`
      Links        []Link       `json:"links"`
      Navigation   []Link       `json:"navigation,omitempty"` // post-correction Compact Collection
      Publications []Publication `json:"publications,omitempty"`
      Facets       []FacetGroup `json:"facets,omitempty"`     // NEW
  }
  type FacetGroup struct {
      Metadata FacetMetadata `json:"metadata"`
      Links    []Link        `json:"links"`
  }
  type FacetMetadata struct { Title string `json:"title"` }
  ```
  Étendre `Link` pour porter `Properties` :
  ```go
  type Link struct {
      Rel       interface{}    `json:"rel,omitempty"`
      Href      string         `json:"href"`
      Type      string         `json:"type,omitempty"`
      Title     string         `json:"title,omitempty"`
      Templated bool           `json:"templated,omitempty"`
      Properties *LinkProperties `json:"properties,omitempty"` // NEW
  }
  type LinkProperties struct {
      NumberOfItems int `json:"numberOfItems,omitempty"`
  }
  ```

  **Catalog API** : ajouter `catalog.Facets(query catalog.SearchQuery) (FacetCounts, error)` qui renvoie en une passe les comptages pour chaque bucket d'intérêt. La SearchQuery passée est celle du contexte courant (tag, author, unread, etc.) — les facets reflètent ce qui reste filtrable APRÈS application des filtres déjà actifs. Exemple :
  ```go
  type FacetCounts struct {
      AgeRating map[int]int   // 0 → unclassified, 3, 6, 10, 12, 16, 18
      Spice     map[int]int   // 0..5
  }
  ```
  - sqlite : un seul `SELECT age_rating, COUNT(*) FROM books WHERE <filters> GROUP BY age_rating` + idem pour spice (filtré sur age >= 16).
  - fs : itère la slice en mémoire avec un compteur ; OK jusqu'à plusieurs milliers de livres.
  Le filtre MaxAgeRating de l'utilisateur enfant est appliqué — la map AgeRating ne contient alors que 3/6/10 et la map Spice est vide.

  **Wiring sur les handlers** : `handleOPDS2Publications`, `handleOPDS2Unread`, `handleOPDS2AuthorBooks`, `handleOPDS2TagBooks`, `handleOPDS2PublisherBooks` appellent `s.catalog.Facets(...)` après leur `Search(...)`, transforment en `[]FacetGroup` via une nouvelle helper `buildFacetGroups(counts, currentQuery, baseURL, tok)` qui :
  - construit un FacetGroup "Classification d'âge" avec un Link par age_rating non vide. `href` = baseURL + querystring courante avec `age_rating=N` ajouté/remplacé. `title` = label humain ("3+", "Tous publics", "Adulte 18+", "Non classifié" pour 0). `properties.numberOfItems` = count.
  - construit un FacetGroup "Piment" si l'utilisateur n'est pas en profil enfant ET au moins un livre 16+/18+ existe. Un Link par valeur 0..5 effectivement présente. `href` = baseURL avec `spice=N`. `title` = label ("Sans", "Suggestif", …). `properties.numberOfItems` = count.
  - n'ajoute PAS un FacetGroup vide.

  **Effets collatéraux** :
  - Les nav items "Niveaux de piment" du root v2 deviennent redondants. Garder dans la nav root pour une release, marquer deprecated dans le code, retirer ensuite.
  - `/opds/v2/spice` peut rester comme alias rétrocompatible mais perd son rôle principal.
  - L'UI Vue interne n'est PAS impactée — c'est purement pour les clients OPDS externes (Foliate, Thorium, Aldiko).

  **Tests** :
  - GET /opds/v2/publications renvoie un champ `facets` avec au moins le groupe "Classification d'âge" quand le catalogue contient des livres classifiés.
  - Le groupe "Piment" est absent quand le caller est un profil enfant.
  - Les hrefs des liens facet préservent le filtre courant (ex: appel `?tag=Fantasy` → les facets pointent vers `?tag=Fantasy&age_rating=N`).
  - `properties.numberOfItems` correspond bien au nombre de livres après application du nouveau filtre (cohérence vérifiée par un second appel filtré).
  - Aucun groupe vide n'apparaît.
  - Spec compliance : chaque FacetGroup a `metadata.title` non vide, et `links` est une compact collection de Link Objects.

- [x] OPDS v2 : exposer pile-à-lire / recos / non-lus / derniers ajouts en `groups` sur la racine — **Done: `internal/opds2/feed.go` — ajout des types `Group{Metadata, Links, Navigation, Publications}` et `GroupMetadata{Title, NumberOfItems omitempty}` ; ajout du champ `Groups []Group` à `Feed` (`json:"groups,omitempty"`). Par spec OPDS 2.0 §2.5, un Group est SOIT navigation SOIT acquisition — jamais les deux (vérifié par un test dédié). `internal/server/handlers.go` — nouvelle constante `rootGroupLimit = 10` (taille max d'un carousel), nouvelle helper `publicationsFromBooks(books, tok)` qui factorise la conversion via bookToPublication, et nouvelle méthode `Server.buildRootGroups(r, tok)` qui assemble les 4 groupes : (1) « Pile à lire » — uniquement si userID identifié ET la pile n'est pas vide ; post-filtre profil enfant qui clip les livres > MaxAge ; `NumberOfItems` = total réel (pas len(Publications)) ; self-link `/opds/v2/to-read` ; (2) « Recommandations reçues » — uniquement si userID identifié ET au moins une reco (réutilise `recommendedBooks(uid)`) ; clip profil enfant ; self-link `/opds/v2/recommendations` ; (3) « Derniers ajoutés » — toujours présent quand le catalog n'est pas vide ; `Search(SortBy:added, SortOrder:desc, Limit:10, MaxAgeRating: ...)` ; self-link `/opds/v2/publications?sort=added_desc` ; (4) « Non lus » — uniquement si total > 0 ; `Search(UnreadOnly:true, SortBy:added, ...)` ; self-link `/opds/v2/unread`. Les groupes vides sont skippés (spec §2.5 violation sinon). Pas de concurrence pour l'instant : 4 Search() séquentiels suffisent sur SQLite local, on basculera en errgroup+timeout si TTFB devient problématique. `handleOPDS2Root` appelle `feed.Groups = s.buildRootGroups(r, tok)` à la fin avant `writeOPDS2`. 4 nouveaux tests : `TestHandleOPDS2Root_GroupsLatestOnEmptyLib` (single-user fresh install → seul « Derniers ajoutés » présent, pas de Pile/Recos, self-link rel="self" vers /opds/v2/publications, vérifie que chaque Group a navigation OU publications), `TestHandleOPDS2Root_GroupsToReadForUser` (multi-user authentifié avec 1 livre dans sa pile → « Pile à lire » présent avec NumberOfItems=1 et self-link /opds/v2/to-read), `TestHandleOPDS2Root_GroupsNoEmptyEntries` (vérifie spec §2.5 — aucun Group ne peut être vide et chaque Group a un title), `TestHandleOPDS2Root_GroupsHideAdultForChild` (profil enfant MaxAge=10 + livre 18+ ajouté → aucun groupe ne contient le livre Spicy). MCP `serverInfo.version` 1.132.0 → 1.133.0. v1.133.0** **Cantook rend chaque acquisition group comme une carousel horizontale de couvertures sur l'écran d'accueil**, avec le titre du group au-dessus et un lien "Voir tout →" qui suit `links.self`. C'est exactement l'UX qu'on veut. Au lieu d'obliger l'utilisateur à naviguer dans 4 sous-feeds successifs, la racine `/opds/v2` renvoie directement N petites sections + un lien "voir tout" sur chacune. Refondre `handleOPDS2Root` (`internal/server/handlers.go:2990-3036`).

  **Type Go (`internal/opds2/feed.go`)** :
  ```go
  type Feed struct {
      Metadata     FeedMetadata `json:"metadata"`
      Links        []Link       `json:"links"`
      Navigation   []Link       `json:"navigation,omitempty"`
      Publications []Publication `json:"publications,omitempty"`
      Facets       []FacetGroup `json:"facets,omitempty"`
      Groups       []Group      `json:"groups,omitempty"`        // NEW
  }
  type Group struct {
      Metadata     GroupMetadata `json:"metadata"`
      Links        []Link        `json:"links,omitempty"`         // self link towards the dedicated feed
      Navigation   []Link        `json:"navigation,omitempty"`    // navigation group
      Publications []Publication `json:"publications,omitempty"` // acquisition group
  }
  type GroupMetadata struct {
      Title         string `json:"title"`
      NumberOfItems int    `json:"numberOfItems,omitempty"`
  }
  ```
  Per la spec : un Group est SOIT navigation, SOIT acquisition (pas les deux).

  **Contenu de la racine `/opds/v2` après refonte** :

  La racine conserve `navigation` (le main menu actuel : Tous les livres / Par auteur / Par genre / Par éditeur / Niveaux de piment) et y ajoute `groups` :

  1. **Groupe "Pile à lire"** — `publications` = 10 premiers items de la pile de l'utilisateur courant ; `links.self` = `/opds/v2/to-read?token=…` ; `metadata.title = "Pile à lire"`, `numberOfItems` = total. Affiché seulement si l'utilisateur a une session ou un token user, ET sa pile n'est pas vide.

  2. **Groupe "Recommandations reçues"** — `publications` = 10 dernières recos reçues ; `links.self` = `/opds/v2/recommendations?token=…` ; affiché seulement en mode multi-user ET si l'utilisateur a au moins 1 reco.

  3. **Groupe "Derniers ajoutés"** — `publications` = 10 livres avec `added_desc` ; `links.self` = `/opds/v2/publications?sort=added_desc&token=…` (limite restreinte aux 10 premiers). Toujours présent.

  4. **Groupe "Non lus"** — `publications` = 10 non-lus tirés par `added_desc` ; `links.self` = `/opds/v2/unread?token=…` ; affiché seulement si l'utilisateur a au moins 1 livre non lu.

  Skip systématique des groupes vides. Le profil enfant voit les groupes filtrés par MaxAgeRating (les requêtes Search() le passent déjà).

  **Helper** : nouvelle fonction `buildGroupAcquisition(title string, limit int, q catalog.SearchQuery, selfHref, tok string) (opds2.Group, error)` qui factorise les 4 appels — query Search + bookToPublication + Group{Metadata,Links,Publications}.

  **Concurrence** : 4 requêtes Search sur la racine, à exécuter en parallèle via une errgroup pour ne pas allonger le TTFB. Chaque sous-call a un timeout court (1 s ?) — un sous-feed lent ne doit pas bloquer la racine ; en cas de timeout, on log et on omet le groupe.

  **Compat clients** : les clients OPDS 2 qui n'implémentent pas `groups` (anciens Aldiko, KOReader) doivent continuer à voir `navigation` et trouver leurs sous-feeds via les entrées existantes. La spec dit `groups` est optionnel, donc OK.

  **Tests** :
  - Multi-user, utilisateur authentifié avec 5 livres dans sa pile → racine contient un Group "Pile à lire" avec `publications.length == 5`, `metadata.numberOfItems == 5`, `links[0].rel == "self"` pointant vers `/opds/v2/to-read?token=…`.
  - Single-user fresh install → racine contient seulement "Derniers ajoutés" (autres groupes absents car vides).
  - Profil enfant → racine ne contient AUCUN livre 16+/18+ dans les groupes (vérifier `bookToPublication` sur tous les groupes).
  - Aucun Group sans `publications` ni `navigation` (spec viole sinon).
  - Le `numberOfItems` du groupe correspond au total réel, pas à `len(publications)` (qui est borné à 10).

- [x] OPDS v2 : nettoyer les sous-feeds redondants et aligner la search sur la spec — **Done: `handleOPDS2Root` (`internal/server/handlers.go`) — la liste `navigation` ne contient plus que les 4 sous-feeds essentiels (Tous les livres → /opds/v2/publications, Par auteur → /opds/v2/authors, Par genre → /opds/v2/tags, Par éditeur → /opds/v2/publishers). Suppressions : « Non lus » (surfacé en Group), « Niveaux de piment » (surfacé en Facet), « Liste de souhaits », « Recommandations », « Pile à lire » (+ ses variantes multi-user via `appendToReadV2NavItems`). La méthode `appendToReadV2NavItems` est supprimée puisqu'elle n'est plus appelée. Les routes correspondantes restent enregistrées dans `server.go` et continuent à répondre 200 pour les clients qui les ont en favoris ; les handlers `handleOPDS2SpiceLevels` et `handleOPDS2ToRead` portent un commentaire `// Deprecated since v1.134` documentant le statut. Nouvelle var package-level `opds2SearchLink = opds2.Link{Rel:"search", Href:"/opds/v2/search{?query}", Type:opds2.MIMEFeed, Templated:true}` — le nom de variable du template est désormais le canonique `query` (spec §2.3.1 + test.opds.io). `handleOPDS2Search` lit `?query=` en priorité et retombe sur `?q=` pour la compat de la SPA Vue ; le message d'erreur 400 fait référence au nouveau nom de paramètre. Le lien templated est exposé via `opds2SearchLink` dans `links[]` de **toutes** les 14 routes OPDS v2 : root, search, publications, unread, all the sub-feeds par auteur/tag/éditeur, listing par auteur/tag/éditeur, spice, to-read, wishlist, recommendations — convention OPDS pour que la barre de recherche du reader reste visible depuis n'importe quel sous-catalogue. Le test obsolète `TestHandleOPDS2Root_HasSpiceNavItem` est remplacé par `TestHandleOPDS2Root_NavigationStripsDeprecatedSubFeeds` qui assert (a) aucun href ne contient `/opds/v2/{spice,unread,to-read,recommendations,wishlist}` dans `navigation`, (b) les 4 essentiels sont présents, (c) les routes dépréciées répondent toujours 200 (back-compat). 3 nouveaux tests : `TestHandleOPDS2Search_AcceptsCanonicalQueryVariable` (`?query=hobb` → 1 hit), `TestHandleOPDS2Search_FallsBackOnLegacyQ` (`?q=hobb` → 1 hit), `TestHandleOPDS2_SearchLinkPresentOnSubFeeds` (rel=search + templated:true + href contient `{?query}` sur les 7 routes principales). MCP `serverInfo.version` 1.133.0 → 1.134.0. v1.134.0**

  **Sous-feeds à retirer du `navigation` racine (mais GARDER les routes en arrière-compat pour 1-2 releases)** :
  - `/opds/v2/spice` — remplacé par le FacetGroup "Piment"
  - `/opds/v2/unread` — exposé désormais comme Group sur la racine + reste accessible via `?spice=…` / `?age_rating=…` sur `/publications`
  - `/opds/v2/to-read` — exposé comme Group ; le lien dans nav root devient redondant
  - `/opds/v2/recommendations` — idem
  - `/opds/v2/wishlist` — voir la tâche dédiée plus bas (retrait complet v1+v2)

  Pour chaque route dépréciée :
  1. Retirer l'entrée correspondante de `handleOPDS2Root` (champ `navigation`).
  2. Conserver la route enregistrée dans `server.go` qui répond toujours pour les clients existants.
  3. Ajouter un commentaire `// deprecated since vX.Y: feed surfaced via groups / facets on /opds/v2` dans le handler.
  4. Une release plus tard : suppression complète du handler + la route.

  Garder dans `navigation` racine seulement les sous-feeds qui restent essentiels :
  - "Par auteur" → `/opds/v2/authors`
  - "Par genre" → `/opds/v2/tags`
  - "Par éditeur" → `/opds/v2/publishers`
  - "Tous les livres" → `/opds/v2/publications` (entry-point pour les facets)

  **Search templated link conforme à la spec** — la spec OPDS 2.0 utilise l'exemple :
  ```json
  { "rel": "search", "href": "search{?query}", "type": "application/opds+json", "templated": true }
  ```
  Le template variable name canonique est `query`. Actuellement la racine émet `/opds/v2/search{?q}` et `handleOPDS2Search` lit `r.URL.Query().Get("q")`. Aligner :
  - `handleOPDS2Search` (`internal/server/handlers.go:3148-3189`) lit `r.URL.Query().Get("query")` en priorité ET retombe sur `Get("q")` pour la compat SPA Vue (`web/index.html` `loadBooks()` envoie `?q=`).
  - Le link templated dans `handleOPDS2Root` devient `{Rel: "search", Href: "/opds/v2/search{?query}", Type: opds2.MIMEFeed, Templated: true}`.
  - Aussi exposer la search link au TOP-LEVEL de `/opds/v2/publications` et tous les autres feeds OPDS v2 (pas seulement la racine) — c'est la convention OPDS pour que la barre de recherche du reader reste visible depuis n'importe quel sous-catalogue.

  **Tests** :
  - GET `/opds/v2` n'expose plus `/spice`, `/unread`, `/to-read`, `/recommendations`, `/wishlist` dans `navigation`. Garde `/authors`, `/tags`, `/publishers`, `/publications`.
  - GET `/opds/v2/search?query=hobb` retourne 200 avec les bons résultats.
  - GET `/opds/v2/search?q=hobb` (legacy SPA) retourne 200 aussi (compat préservée).
  - Le link `rel:"search"` apparaît dans `links` de toutes les pages OPDS v2 (racine, publications, unread, author-books, etc.).
  - Les routes dépréciées (`/spice`, `/to-read`, …) répondent toujours 200 pour les clients existants.

- [x] OPDS v2 : conformité du `bookToPublication` pour Cantook/Aldiko — **Done: `internal/opds2/feed.go` — `Link` gagne deux champs optionnels `Width int` / `Height int` (`omitempty`) pour porter les dimensions des entrées `images[]` ; valeurs émises uniquement quand non-zéro, donc rétrocompatible (aucun champ de plus dans la JSON quand on ne sait pas — extraction PIL/`image.DecodeConfig` sur les covers est laissée pour un loop futur quand on persistera les dimensions sur `catalog.Book`). `internal/server/handlers.go` — la signature `bookToPublication(b, tok)` devient `bookToPublication(b, tok, baseURL string)` ; nouvelle helper `absURL(baseURL, href)` qui préfixe les URLs relatives (laisse intactes celles déjà absolues `http://`, `https://`, `data:`, ou retourne href verbatim quand baseURL=""). Les **4 emplacements** identifiés dans le plan sont mis à jour pour produire des URLs absolues : les deux entrées de `images[]` (cover + thumbnail) et l'acquisition link. Le **rel** des images est retiré (« images[].rel » → absent ; suit la convention test.opds.io/2.0/home.json) ; le rel de l'acquisition link passe de `http://opds-spec.org/acquisition` à `http://opds-spec.org/acquisition/open-access` (Cantook affiche un bouton « Télécharger » direct au lieu de demander confirmation). Les 9 call-sites de `bookToPublication` (+ le helper `publicationsFromBooks` utilisé par `buildRootGroups`) sont mis à jour pour passer `externalURL(r)` — `buildRootGroups` reçoit `r` en argument et calcule baseURL une fois. `externalURL` (déjà utilisé par le pairing librarian) honore `X-Forwarded-Proto` / `X-Forwarded-Host` donc le rendu derrière reverse proxy reste correct. 2 nouveaux tests : `TestBookToPublication_CantookConformity` vérifie qu'un GET `/opds/v2/publications` sur `books.example.com` retourne une Publication dont chaque `images[]` n'a PAS de rel et a un href absolu (`http://books.example.com/...`), qu'au moins un link d'acquisition a `rel:"http://opds-spec.org/acquisition/open-access"` avec un href absolu, et que l'ancien rel bare `http://opds-spec.org/acquisition` n'apparaît plus ; `TestAbsURL_LeavesAlreadyAbsoluteUnchanged` couvre les 7 cas du helper absURL (relatif → préfixé, http/https/data → laissé intact, baseURL vide → no-op, href vide → vide, sans slash leading → slash ajouté). Items "bonus" du plan reportés (Author.SortAs nécessite un champ Sort sur catalog.Author + extraction du content.opf `<file-as>` ; belongsTo.series single-object syntactique ; numberOfPages nécessite spine-count à l'extraction — chacun est un mini-loop à part). MCP `serverInfo.version` 1.134.0 → 1.135.0. v1.135.0** Comparaison avec https://test.opds.io/2.0/home.json (catalogue Feedbooks de référence) :

  **1. Retirer `Rel:"http://opds-spec.org/image"` des entrées `images[]`.** La référence émet ses entrées d'images SANS rel — juste `{href, type, width, height}`. Pattern moderne OPDS 2. (Un livre de la référence garde le rel OPDS 1 par compat, mais tous les autres l'omettent — l'omission est le standard.)

  **2. Ajouter `width` / `height` sur les liens d'image.** La référence émet systématiquement deux variantes :
  ```json
  "images": [
    {"href": ".../small.jpg",  "type": "image/jpeg", "height": 169, "width": 110},
    {"href": ".../normal.jpg", "type": "image/jpeg", "height": 400, "width": 260}
  ]
  ```
  Cantook les utilise pour choisir la bonne taille selon le contexte (carousel / liste / fiche). Étendre `opds2.Link` :
  ```go
  type Link struct {
      ... // existing
      Width    int `json:"width,omitempty"`
      Height   int `json:"height,omitempty"`
  }
  ```
  Si dimensions inconnues (cover uploadée), tenter `image.DecodeConfig` sur le fichier au moment de l'extraction et persister width/height sur `catalog.Book`.

  **3. URLs absolues pour `images[]` et acquisition links.** Certains Cantook (versions Android anciennes) résolvent mal les URLs relatives. Construire l'URL absolue via `externalURL(r)` (helper déjà utilisé pour le pairing) en respectant `X-Forwarded-Proto` / `X-Forwarded-Host` derrière reverse proxy. Appliquer aux 4 endroits :
  - `withToken(b.CoverURL, tok)` → `externalURL(r) + withToken(b.CoverURL, tok)`
  - `withToken(b.ThumbnailURL, tok)` → idem
  - L'acquisition link `/opds/books/{id}/download?path=…` → idem
  - Les self-link de Publication (si présent)

  Pour faire ça proprement sans casser la signature, passer `r *http.Request` à `bookToPublication(b, tok, r)` (ou pré-calculer `baseURL` une fois et le passer en paramètre — moins intrusif).

  **4. Acquisition link rel.** L'actuel `Rel:"http://opds-spec.org/acquisition"` est correct (acquisition générique / téléchargement). Cantook reconnaît aussi `acquisition/open-access` (gratuit), `acquisition/buy`, `acquisition/borrow`. Comme la bibliothèque nxt-opds est personnelle = libre accès pour l'utilisateur authentifié, utiliser **`http://opds-spec.org/acquisition/open-access`** plutôt qu'`acquisition` tout court : Cantook le présente alors avec un bouton « Télécharger » direct au lieu de demander confirmation.

  **5. Publication self-link.** Ajouter un `Rel:"self"` sur chaque Publication pointant vers `/opds/v2/publications/{id}` (nouvelle route) qui renvoie la même Publication isolée dans son propre feed — Cantook l'utilise pour la fiche détail. Si ajouter la route est trop lourd, omettre le self-link suffit (Cantook se rabat sur les images + metadata).

  **Bonus métadonnées (présents dans la référence)** :
  - `metadata.author` accepte `{"name": "X", "sortAs": "Y, X"}` — utiliser `sortAs` quand l'auteur a `LastName, FirstName` connu (catalog.Author a un champ Sort). Cantook trie alpha sur sortAs.
  - `metadata.subtitle` quand le titre a un sous-titre distinct (rare dans nxt-opds, optionnel).
  - `metadata.numberOfPages` si calculé depuis l'EPUB (page count). Optionnel.
  - `metadata.belongsTo.series` peut être un OBJET direct quand il n'y a qu'une série : `{"name": "...", "position": N}` au lieu d'un tableau d'un élément. Sérialisation plus compacte, autorisée par la spec.
  - `subject` accepte `{"name": "X", "code": "BISAC_CODE"}` — Cantook reconnaît les codes BISAC pour grouper. Notre catalog ne stocke pas les codes, donc on garde juste `name`.

  **Tests** :
  - GET `/opds/v2/publications` : chaque publication dans `publications[]` a `images[].href` absolu (commence par `http://` ou `https://`).
  - Aucun `rel` sur les éléments de `images[]`.
  - `width` et `height` présents quand disponibles (ne pas casser quand absents).
  - L'acquisition link a `rel="http://opds-spec.org/acquisition/open-access"`.
  - Quand `Author.Sort != ""`, l'auteur est sérialisé en objet `{name, sortAs}`.
  - Comparer manuellement la sortie sur 3 livres avec la structure de https://test.opds.io/2.0/home.json — alignement field-by-field.
  - Tester l'ouverture du catalogue dans **Cantook** (Android ou iOS) avec un utilisateur authentifié et vérifier : (a) couvertures s'affichent (deux tailles), (b) bouton Télécharger direct sur la fiche, (c) facets+groups rendus correctement, (d) auteurs triés alphabétiquement (sortAs respecté).

- [x] Retirer la wishlist des flux OPDS v1 et v2 — **Done: la wishlist n'est plus exposée dans les flux OPDS — elle reste accessible via le SPA Vue, l'API REST (`/api/wishlist*`) et le MCP (`list_wishlist`, `add_wishlist_item`, `delete_wishlist_item`). Concrètement : `internal/server/handlers.go` — entrée nav root « Liste de souhaits » retirée de `handleOPDSRoot` (la branche `if s.wishlistManager != nil { feed.AddEntry(...) }` supprimée) ; fonction `handleOPDSWishlist` complète supprimée ; section comment « OPDS Wishlist / Recommendations » renommée en « OPDS Recommendations » ; fonction `handleOPDS2Wishlist` complète supprimée (la branche d'ajout root OPDS v2 avait déjà été retirée en v1.134 — la fonction handler restait orpheline). `internal/server/server.go` — les deux routes mux supprimées : `protected.HandleFunc("/opds/wishlist", s.handleOPDSWishlist)` et `protected.HandleFunc("/opds/v2/wishlist", s.handleOPDS2Wishlist)`. Test mis à jour : `TestHandleOPDS2Root_NavigationStripsDeprecatedSubFeeds` ne s'attend plus à 200 pour `/opds/v2/wishlist` (kept = spice + unread uniquement) ; nouveau bloc à la fin du test qui assert que `GET /opds/wishlist` ET `GET /opds/v2/wishlist` retournent désormais 404 — confirmant que les routes ne sont plus accessibles aux clients OPDS qui les avaient en favoris (les wishlists ne sont pas des publications téléchargeables, donc les exposer en OPDS embrouillait Cantook/Aldiko). README mis à jour : ligne `GET /opds/wishlist | Wishlist feed` supprimée de la table OPDS Endpoints ; les mentions MCP/API REST restent inchangées. Tous les tests existants `TestWishlist*` côté REST + `TestToolListWishlist*` côté MCP passent sans modification — la wishlist côté APIs internes est intacte. MCP `serverInfo.version` 1.135.0 → 1.136.0. v1.136.0** Sa présence dans le catalogue OPDS embrouille les clients (Cantook affiche des "livres" qu'on ne peut pas télécharger) et n'apporte rien fonctionnellement : elle reste accessible via le SPA Vue + l'API REST + le MCP (list_wishlist / add_wishlist_item / delete_wishlist_item). À supprimer :

  **OPDS v1** (`internal/server/handlers.go`) :
  - L'entrée nav root « Liste de souhaits » dans `handleOPDSRoot` (~lignes 228-236) qui pointe vers `/opds/wishlist` → retirer la branche `if s.wishlistManager != nil`.
  - Le handler `handleOPDSWishlist` (~ligne 2512) → supprimer la fonction.
  - La route `/opds/wishlist` dans `server.go` → retirer la ligne.

  **OPDS v2** (`internal/server/handlers.go`) :
  - L'entrée nav root dans `handleOPDS2Root` (~ligne 3132 — `if s.wishlistManager != nil`) → retirer la branche.
  - Le handler `handleOPDS2Wishlist` (~ligne 2658) → supprimer la fonction.
  - La route `/opds/v2/wishlist` dans `server.go` → retirer la ligne.

  **À conserver intactes** (NE PAS toucher) :
  - `GET /api/wishlist`, `POST /api/wishlist`, `PATCH/DELETE /api/wishlist/{id}` (REST API SPA)
  - Tools MCP `list_wishlist`, `add_wishlist_item`, `delete_wishlist_item`
  - Vue #/wishlist page + bouton header
  - Le `wishlistManager` côté catalog (toujours nécessaire pour les API/MCP)

  README à mettre à jour pour retirer les références aux flux OPDS wishlist. Tests : GET `/opds/wishlist` retourne 404, GET `/opds/v2/wishlist` retourne 404, ni `/opds` ni `/opds/v2` ne mentionnent "Liste de souhaits" dans leur navigation. L'API REST + MCP wishlist passent toujours tous leurs tests existants sans modification.

- [x] MCP : auto-résoudre user_id depuis l'auth sur les tools user-scopés — **Done: new `mcp.UserResolver` callback type + `Server.SetUserResolver()` so the host (server package) tells the MCP server which user is authenticated on each request; `internal/server/server.go` wires `s.mcpUserResolver` which reads `currentUserID(r)` (already injected by authMiddleware via the per-user OPDS token) and looks up `IsAdmin` via UserManager.UserByID. Inside `ServeHTTP`, a new `callContext{UserID, IsAdmin}` is built from the resolver and passed through `handleToolsCall` to each user-scoped tool. New `resolveUserScope(cc, args, "user_id")` helper centralises the precedence rules: (1) `args["user_id"]` non-empty → use it but require admin when it differs from `cc.UserID`; (2) empty → fall back to `cc.UserID`; (3) both empty → explicit error message asking the caller to authenticate or pass user_id explicitly. The 4 to-read tools (`list_to_read`, `add_to_read`, `remove_to_read`, `reorder_to_read`) use `resolveUserScope` directly. `add_wishlist_item`, `list_wishlist`, `list_recommendations` and `set_book_read` use tailored logic since they have a special "no userID = all users / global is_read" behaviour: a non-admin authenticated caller without user_id implicitly filters to their own scope, while admins / shared-token callers retain the legacy "all users" view. `delete_wishlist_item` (no user_id arg) gains an ownership check: when caller is a non-admin authenticated user, the targeted wishlist ID must appear in their own list. Schemas updated: `user_id` removed from `Required` on the 4 to-read tools, descriptions changed to « Optionnel — défaut: utilisateur authentifié. Admin uniquement pour … d'un autre utilisateur. ». MCP `serverInfo.version` bumped 1.99.0 → 1.121.0. 9 new tests in server_test.go cover auto-resolution (add_to_read/list_to_read/set_book_read/add_wishlist_item), unauthenticated-no-user_id error, cross-user-non-admin 403 (add_to_read + set_book_read + delete_wishlist_item), and cross-user-admin happy path. v1.121.0**
- [x] commit et pousse la dernier mise a jour — **Done: bundled three related changes into v1.137.0. (1) sqlite `Refresh()` no longer auto-stamps `last_maintenance_at` on discovered books — librarian skill is now the sole writer (the helper `updateMaintenanceAt` is also removed since `StoreBook` had already stopped calling it in v1.81). (2) PATCH /api/books/{id} `lastMaintenanceAt: 0` clears the field so the book re-enters the not-indexed queue (comment in `bookUpdateRequest` documents the contract, behaviour itself was already supported by `sqlite.UpdateBook`). (3) Vue book detail page gains a small ✕ button next to « Indexé le DD/MM/YYYY HH:MM » that, after a confirm dialog, PATCHes `lastMaintenanceAt: 0` and refreshes the page state via the existing `apiFetch` helper — `clearingMaintenance` ref disables the button while in-flight. Test rewrite in `sqlite_test.go` asserts: (a) all books are `LastMaintenanceAt.IsZero()` immediately after `Refresh()`, (b) `NotIndexed` filter returns both books, (c) `UpdateBook(LastMaintenanceAt=now)` removes one from the not-indexed set, (d) `UpdateBook(LastMaintenanceAt=time.Time{})` brings it back — the round-trip is the contract surface the new UI relies on. MCP `serverInfo.version` 1.136.0 → 1.137.0. v1.137.0**

## Medium Priority
- [x] Add EPUB upload endpoint (POST /api/upload) with file storage + instant catalog indexing - **Done: StoreBook on fs.Backend, handleUpload + handleDownload handlers**
- [x] Add test coverage for opds feed serialization - **Done: internal/opds/feed_test.go (5 tests covering structure, XML serialization, round-trip, date formatting, multiple entries)**
- [x] Add test coverage for HTTP handlers - **Done: internal/server/handlers_test.go (34 tests covering all OPDS routes, API endpoints, pagination helpers, update book); also fixed auth.go slice-bounds panic and missing catch-all route in server.go**
- [x] Add pagination link headers (first/last/next/prev) to feeds - **Done: RelFirst/Last/Next/Previous consts in opds/feed.go; addPaginationLinks helper in handlers.go; applied to all 6 paginated OPDS handlers**
- [x] Add cover image serving endpoint - **Done: EPUB cover extraction (EPUB2+EPUB3), /covers/{id} HTTP endpoint, CoverProvider interface**
- [x] Add password authentication with login form - **Done: session-cookie auth, POST /login form, GET /login page (Tailwind), POST /logout, Basic Auth fallback for OPDS readers, HttpOnly cookie, constant-time comparison, 30-day sessions**
- [x] Vue+Tailwind CSS frontend - **Done: web/index.html (CDN Vue3+Tailwind CSS), embedded via Go embed.FS, served at /, GET /api/books JSON endpoint, Feedbooks-style book grid with covers, search, upload dialog, detail dialog, dark mode, color gradients for missing covers, pagination**
- [x] Add configuration file support (YAML/TOML) - **Done: internal/config/config.go with Config struct, Load() (YAML file + env var override), FindConfigFile() (NXT_OPDS_CONFIG env, ./nxt-opds.yaml, ~/.config/nxt-opds/config.yaml); main.go updated; gopkg.in/yaml.v3 added**
- [x] Book metadata editing (title, authors, tags, series via PATCH /api/books/{id}) - **Done: metaOverride JSON store (.metadata.json), Updater interface, fs.Backend.UpdateBook, PATCH /api/books/{id} handler**
- [x] Series support: add Series/SeriesIndex fields to Book, display in UI - **Done: Book.Series/SeriesIndex fields, shown in detail modal, editable via edit modal**
- [x] "Has been read" mark: toggle via PATCH /api/books/{id}, show indicator in UI - **Done: Book.IsRead field, green badge on card, checkbox in edit modal**
- [x] Pagination on front page grid - **Done: PAGE_SIZE=48 constant, page/totalPages/pageNumbers computed refs, goPage() navigator, offset+limit params on /api/books, prev/next/ellipsis pagination widget in grid view**
- [x] Add an OPDS 2.0 feed that follow https://drafts.opds.io/opds-2.0 - **Done: internal/opds2/feed.go (types + JSON serialization); 7 handlers in handlers.go (root, publications, search, authors, author-books, tags, tag-books); routes registered in server.go at /opds/v2/**
- [x] Add a github action that build the docker and push it to docker hub - **Done: .github/workflows/docker.yml – triggers on push to main and version tags; multi-platform (amd64+arm64); uses DOCKERHUB_USERNAME/DOCKERHUB_TOKEN secrets; semantic version tags + sha tags; GHA cache**
- [x] Add a github action that build binary and release it on github repository - **Done: .github/workflows/release.yml – triggers on version tags; matrix: linux/amd64, linux/arm64, darwin/amd64, darwin/arm64, windows/amd64; CGO_ENABLED=0; SHA256SUMS.txt; uses softprops/action-gh-release@v2**
- [x] Add a button delete on book page - **Done: catalog.Deleter interface; DeleteBook on fs+sqlite backends (removes file + cover); DELETE /api/books/{id} handler; red "Supprimer" button with trash icon + confirmation dialog in book detail page; navigates back to library on success**
- [x] ajust size cover to the frame - **Done: CSS book-cover uses absolute-positioned img with object-fit:cover for correct frame fill**
- [x] Add "series title - #number/#totalnumber" to the frontpage between the title and the author - **Done: amber series line shown in grid card meta between title and author**
- [x] Add "series title - #number/#totalnumber" to the book page between the title and the author instead of badge - **Done: inline amber text below title, SeriesTotal field added throughout stack**
- [x] Add total series to the book metadata editing - **Done: SeriesTotal field in catalog.Book/BookUpdate, fs/sqlite backends, handlers.go, edit modal**
- [x] Add series page sort by number - **Done: catalog.SeriesLister interface; Series() on fs+sqlite backends; GET /api/series endpoint; ?series= + ?sort=series_index params on /api/books; series page view (#/series/<name>) in Vue SPA; clickable series links on grid card and book detail; "Tome N/Total" badge in series page; books sorted numerically by series_index**
- [x] Add star rating - **Done: Rating int field (0=unrated, 1-5) throughout stack (catalog, fs, sqlite, handlers); interactive 5-star widget on book detail page (click same star to clear); small read-only stars on grid cards**
- [x] Ajout d'un token d'authentification pour les flux OPDS à la place du basic auth - **Done: OPDSToken added to Config (yaml: opds_token / env: OPDS_TOKEN); if not set but password is configured, a stable 32-char hex token is auto-derived via SHA-256(password); authMiddleware now accepts ?token=<opds_token> on OPDS routes instead of Basic Auth (Basic Auth kept as fallback only when no token configured); GET /api/config returns opdsToken for the Vue UI; Vue SPA shows a "copy OPDS URL" clipboard button in the grid header that copies window.origin + /opds?token=xxx; startup logs the OPDS reader URL; 6 new tests**
- [x] Pass the OPDS auth token in OPDS url to avoid 401 on sub url in feed - **Done: withToken(href, tok) helper; bookToEntry/bookToPublication accept tok param; all OPDS v1+v2 handlers extract tok from request and wrap all static hrefs; 9 new tests**
- [x] Pass OPDS auth token to cover - **Done: authMiddleware now accepts ?token= on /covers/{id} paths in addition to /opds/* routes; 3 new tests**
- [x] Make edit form larger - **Done: edit modal container changed from max-w-lg to max-w-2xl**
- [x] Ajoute une page qui liste tout les tags et permettre de supprimer un tag - **Done: catalog.TagDeleter interface; DeleteTag on fs backend (removes from all books, persists via overrides) and sqlite backend (DELETE FROM book_tags); DELETE /api/tags/{tag} handler; Vue #/tags management page lists all tags as blue pills with trash-icon delete buttons + confirmation dialog; "Étiquettes" pill link in grid filter bar for navigation**
- [x] Ajout des boutons de passage vers le livre précédent ou suivant depuis la fiche du livre - **Done: navBookIDs ref stores the ordered ID list of the source list (grid/series/author/tag/publisher/collection); openBook(bookId, contextBooks?) sets context and navigates; navBookIndex/prevBookID/nextBookID computed; header shows ‹ N/Total › buttons when context is available; clicking prev/next keeps same context so navigation is continuous**
- [x] Ajout d'un bouton recommandé pour un utilisateur sur le livre et des pages de recommandations des personnes. - **Done: "Recommander" button on book detail page (multi-user only) opens user-picker dialog; POST /api/books/{id}/recommend handler; GET /api/recommendations handler; DELETE /api/books/{id}/recommend/{toUserID} handler; GET /api/books/{id}/recipients handler; #/recommendations Vue page showing books recommended to current user with sender name/color/message; share icon in header with violet dot badge when recommendations exist; recommendations loaded on startup in multi-user mode**
- [x] Le suivant et précédent doit pouvoir naviguer sur la totalité de la bibliothéque et pas seulement la page en cours. - **Done: ids_only=1 param on GET /api/books returns {ids:[...]} for all matching books without pagination limit; openBook() now async; grid card click passes gridFilterParams() string (q/unread/sort) instead of current page books; IDs are fetched from server before/during navigation so prev/next covers entire filtered library; series/author/tag/publisher/collection views unaffected (already load all books)**
- [x] Pour le passage suivant et précédent gérer le slide tactile pour téléphone et les racourcis claviers fleche gauche et fleche droite. - **Done: keyboard ArrowLeft/ArrowRight handler (onKeyDown) registered on window in onMounted, skips when input/textarea focused; touch swipe handler (onBookTouchStart/onBookTouchEnd) on book detail div (touchstart.passive + touchend), fires when |dx|≥60px and horizontal > vertical movement; swipe-right=prev, swipe-left=next**
- [x] Ajout la fonction de mise a jour automatique du binaire - **Done: internal/updater package (CheckLatest via GitHub API, Apply does atomic rename); GET /api/update/check + POST /api/update/apply endpoints; version injected via -X main.version at build time; Vue SPA: amber icon with pulsing dot when update available, dialog shows old→new version + release notes + "Mettre à jour" button; user told to restart after apply; v1.53.0**
- [x] Le redemarrage ce passe bien mais il est tellement rapide que le frontend ne voit pas qu'il a redemarrer - **Done: added GET /api/ping (public, no auth) returning {ok, startedAt} Unix milliseconds; Server.startedAt set at construction time; restartServer() in Vue now records prevStartedAt before restart, then polls /api/ping and reloads when startedAt changes (true restart detected regardless of how fast the process re-exec's)**
- [x] Le mcp ne se connecte plus et il n'y a aucun log, sur un get sur /mcp j'ai une 404, rajoute au moins une fonction pour lancer en mode DEBUG - **Done: added `Debug` config field (env `DEBUG=1|true|yes` or yaml `debug:`), propagated through `server.Options.Debug` to `mcp.Server.SetDebug` and `authMiddleware`. When enabled, every request logs the auth decision (session, user-token, shared-token, basic, unauthorized) and every MCP JSON-RPC call logs method/id/errors. Added GET /mcp handler returning a small JSON document (name, protocolVersion, transport, endpoint, auth hint) instead of falling through to the SPA catch-all 404. Auth middleware no longer redirects MCP requests to /login (returns 401 cleanly). 2 new tests (GET /mcp returns 200 JSON; GET /mcp without auth returns 401 not redirect). v1.99.0**

## Low Priority
- [x] Performance optimization (background indexing) - **Done: catalog.Refresher interface; background ticker goroutine in main.go (REFRESH_INTERVAL env / refresh_interval config, default 5m); POST /api/refresh manual endpoint; refresh button with spinner in Vue UI header**
- [x] Code cleanup and refactoring - **Done: tests for POST /api/refresh (success, 501, 500 paths); 6 tests for refresh_interval config parsing; updated package doc in config.go; updated Architecture Notes to reflect current package layout**
- [x] Docker / container support - **Done: Dockerfile (multi-stage, debian-slim runtime, CGO_ENABLED=0), .dockerignore, docker-compose.yml, README.md updated with full docs**
- [x] SQLite index for large collections - **Done: internal/backend/sqlite/sqlite.go, selected via backend: "sqlite" in config or BACKEND=sqlite env var; epub metadata extraction refactored into internal/epub/epub.go shared package; 9 tests in sqlite_test.go**
- [x] Make the "has been read" mark on cover more visible - **Done: replaced tiny top-right ✓ pill with a prominent bottom-of-cover green gradient overlay strip showing a bold checkmark + "Lu" text**
- [x] Passer la marque "lu" avec un bandeau en dessous de dégradé de vert - **Done: replaced in-cover overlay strip with a separate green gradient banner (from-green-600 to-emerald-500) rendered below the cover, with rounded bottom corners; cover uses rounded-t-lg when book is read so the pair forms one visual unit**
- [x] Sur la page du livre les étoiles de score ne marche pas le choix précédent n'est pas affiché mais est bien sauvegardé. - **Done: handleAPIBook and handleAPIUpdateBook were missing Rating: bk.Rating in their bookJSON responses; added to both handlers**
- [x] refresh button use basic auth instead of session cookie - **Done: added apiFetch() helper in Vue SPA that checks every API response for HTTP 401 and immediately redirects to /login; replaces all 10 raw fetch('/api/…') calls – loadBooks, loadBook, loadSeries, toggleRead, setRating, saveEdits, deleteBook, onCoverFileChange, doRefresh, doUpload – so a stale/expired session is always handled cleanly (redirect to login) instead of showing a cryptic error toast or triggering a browser Basic-Auth dialog**
- [x] Some cover are not extract from epub because of bad metadata, search the image on first html page - **Done: findCoverInSpine() walks OPF spine in order, opens first HTML/XHTML item, uses findFirstImgSrc() to locate first <img src="…">, saves image as book cover; 9 unit tests in epub_test.go**
- [x] allow update cover - **Done: catalog.CoverUpdater interface; UpdateCover() on fs+sqlite backends (removes old cover files, writes new image, updates DB/in-memory records); POST /api/books/{id}/cover handler (20 MB limit, auto-detects ext from MIME/filename); "Changer la couverture" button in book detail left column (file input ref, cache-busts URL with ?t=timestamp after upload); handleCover now uses actual file mod-time for proper browser cache invalidation**
- [x] Avoir une page des livres d'un auteur, doit aussi être dans les flux OPDS - **Done: Vue SPA #/authors/<name> page; ?author= filter added to /api/books + Search in fs+sqlite backends; GET /api/authors endpoint; author names clickable in grid cards and book detail; OPDS v1/v2 feeds were already implemented**
- [x] Avoir une page des livres d'un editeur, doit aussi être dans les flux OPDS - **Done: Vue SPA #/publishers/<name> page; ?publisher= filter added to /api/books + Search in fs+sqlite backends; GET /api/publishers endpoint; publisher name clickable in book detail page; OPDS v1 (GET /opds/publishers, /opds/publishers/{publisher}) and v2 (GET /opds/v2/publishers, /opds/v2/publishers/{publisher}) feeds; nav entries added to both root feeds**
- [x] Avoir une page des livres d'un tag, doit aussi être dans les flux OPDS - **Done: Vue SPA #/tags/<name> page; ?tag= filter added to /api/books + Search in fs+sqlite backends; GET /api/tags endpoint; tags clickable in book detail page; OPDS v1/v2 feeds were already implemented**
- [x] Avoir une catégorie non lu dans les flux OPDS - **Done: handleUnreadBooks (GET /opds/unread) for OPDS v1; handleOPDS2Unread (GET /opds/v2/unread) for OPDS v2; nav entries added to both root feeds; uses Search(UnreadOnly:true) with added-desc sort**
- [x] Ajoute la collection avec un numéro dans les métadatas - **Done: epub.go parses series/collection from both Calibre-style EPUB2 (<meta name="calibre:series"/>) and EPUB3 OPF3 (<meta property="belongs-to-collection"/>) OPF metadata; populated into Book.Series/SeriesIndex at index time; OPDS v1 Entry gains CalSeries/CalSeriesIndex fields using Calibre namespace (http://calibre.kovidgoyal.net/2009/metadata) included in all acquisition feeds; bookToEntry() populates series in OPDS v1 entries; 9 new tests in epub_test.go**
- [x] La collection doit être modifiable dans le formulaire - **Done: already implemented — edit form has Série/N°/Total fields (editForm.series/seriesIndex/seriesTotal) populated by openEdit(), sent via PATCH /api/books/{id}, and handled in UpdateBook; the collection extraction added in the previous task populates Book.Series/SeriesIndex which flows into and out of the edit form transparently**
- [x] Handle editorial collection - **Done: Collection field on catalog.Book/BookUpdate/SearchQuery; extractCollectionFromMetas() extracts EPUB3 set-type belongs-to-collection; SQLite migration v2 adds collection column; fs backend metaOverride + Search filter; API exposes ?collection= filter + bookJSON.Collection; Vue UI shows collection as clickable link in book detail (purple), editable in edit modal, full #/collections/<name> browse page; 4 new epub tests**
- [x] Add an editorial collection number - **Done: CollectionIndex field added throughout the stack (catalog.Book/BookUpdate, epub.go parses group-position refine, SQLite migration v3 adds collection_index column, fs backend metaOverride, API bookJSON + PATCH handler, Vue UI shows #N beside collection name in book detail and adds N° collection input in edit modal); test updated for new 2-return extractCollectionFromMetas signature**
- [x] Allow to change cover even if there already a cover - **Done: extractCoverFromPkg and findCoverInSpine now use CoverPath() (checks all extensions) instead of single-extension os.Stat; prevents fs backend Refresh from re-extracting EPUB cover when user has uploaded a custom cover with a different extension**
- [x] Ajoute un serveur MCP afin de pouvoir connecter un agent qui pourra faire de la modification de metadata et de la recherche - **Done: internal/mcp/server.go implements JSON-RPC 2.0 over HTTP (MCP 2024-11-05 Streamable HTTP transport); tools: search_books, get_book, update_book, list_authors, list_tags, list_series, list_publishers; POST /mcp endpoint registered in server.go; auth via Authorization: Bearer <opds_token> or ?token= param; 12 tests in mcp/server_test.go; startup logs MCP endpoint URL**
- [x] dans le formulaire proposer l'auto complétion des séries, tag et collection existante. - **Done: catalog.CollectionLister interface added; Collections() on fs+sqlite backends; GET /api/collections endpoint; loadAutocompleteData() fetches tags/series/collections on first openEdit(); series+collection fields use native HTML5 <datalist> (ac-series, ac-collections); tags field has custom dropdown showing filtered suggestions on the current partial word after last comma (selectTag() completes it)**
- [x] Ne pas fermer le formulaire si on clique en dehors du formulare, ne se ferme qu'avec la croix ou cancel. - **Done: removed @click.self="closeEdit" from modal container div and @click="closeEdit" from backdrop overlay div; modal now only closes via the X button or Annuler button**
- [x] Génére moi un favicon du projet - **Done: web/favicon.svg (open-book SVG icon with blue #2563eb brand colour, rounded background, two pages with text lines); embedded via go:embed in web/embed.go; referenced via <link rel="icon"> in index.html**
- [x] Gére une liste de livre rechercher par personne. Il faut pouvoir saisir le titre, l'auteur et la date de sortie. - **Done: WishlistItem+WishlistManager in catalog; SQLite migration v6 (wishlist table); CRUD endpoints GET/POST /api/wishlist + PATCH/DELETE /api/wishlist/{id}; Vue #/wishlist page with per-user color dots, inline edit, delete confirm; bookmark icon in header with amber badge; v1.50.0**
- [x] Permet au serveur mcp d'uploader un livre avec authentification par le token - **Done: upload_book tool on MCP server; accepts filename + base64 content; auth via existing Bearer token / ?token= middleware on /mcp route; StoreBook called via catalog.Uploader; 2 tests (happy path + not supported); v1.51.0**
- [x] Retire le lien recommandation en double - **Done: wishlist button was duplicated in the header (once in grid-only block, once in multi-user block); added v-if="!(multiUser && currentUser)" to the grid-only button so it only shows in single-user mode**
- [x] Genere un fichier de config systemd pour l'utilisation du binaire - **Done: nxt-opds.service unit file (Type=simple, User=nxt-opds, WorkingDirectory=/opt/nxt-opds, all env vars documented, Restart=on-failure, security hardening directives, ReadWritePaths=/var/lib/nxt-opds)**
- [x] La recherche doit aussi prendre en compte les noms des séries - **Done: added LOWER(b.series) LIKE ? to SQLite search JOIN; added series name check in fs backend Search loop; 2 new tests**
- [x] expose la liste de souhait et les recommandations dans les flux OPDS et dans le MCP - **Done: OPDS v1 (GET /opds/wishlist navigation feed, /opds/recommendations acquisition feed); OPDS v2 (GET /opds/v2/wishlist, /opds/v2/recommendations); nav entries added to both root feeds when supported; MCP 4 new tools: list_wishlist, add_wishlist_item, delete_wishlist_item, list_recommendations; v1.55.0**

## Completed
- [x] Project enabled for Ralph
- [x] Bootstrapped Go module (go.mod, main.go)
- [x] OPDS feed types and XML serialization (internal/opds/feed.go)
- [x] Catalog interface and data types (internal/catalog/catalog.go)
- [x] HTTP server and routing (internal/server/server.go)
- [x] HTTP request handlers skeleton (internal/server/handlers.go)
- [x] README.md with API docs
- [x] Filesystem catalog backend (internal/backend/fs/fs.go)
- [x] EPUB OPF metadata extraction (title, author, subject, language, date)
- [x] Catalog wired into server and all handlers populated
- [x] Tests for fs backend (internal/backend/fs/fs_test.go)

## Architecture Notes

### Packages
- `internal/opds` - Pure OPDS/Atom types and XML serialization, no I/O
- `internal/catalog` - Domain model + Catalog interface (backend-agnostic)
- `internal/server` - HTTP server, routes, handlers, auth
- `internal/epub` - Shared EPUB/PDF metadata extraction (archive/zip + encoding/xml)
- `internal/config` - YAML config loading with env-var overrides
- `internal/backend/fs` - In-memory filesystem backend (.metadata.json overrides)
- `internal/backend/sqlite` - SQLite-indexed backend (.catalog.db)
- `web/` - Embedded Vue 3 + Tailwind CSS frontend (go:embed)

### Key Design Decisions
- Catalog backend is injected via interface (testable, swappable)
- Optional interfaces (Uploader, CoverProvider, Updater, Refresher) let backends
  opt-in to additional capabilities; server detects each via type assertion
- OPDS XML serialization uses stdlib encoding/xml
- gorilla/mux for URL parameter extraction
- Handlers are thin; business logic stays in catalog backends
- modernc.org/sqlite is pure Go (CGO_ENABLED=0 works, Docker scratch/slim OK)

## Notes
- Focus on MVP functionality first
- Ensure each feature is properly tested
- Update this file after each major milestone
- Create or increment a release version and tag it on git commit with format v[number].[number]
